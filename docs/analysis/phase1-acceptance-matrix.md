# Phase 1 수용 기준 13개 — 시험 대조표

작성 2026-08-29 · 대상 원장 `docs/spec/00.decision-log.md` §4 (690~712행) · 활성 target `phase-1`

이 표가 답하려는 것은 하나다 — **기준마다 이미 그것을 재는 시험이 있는가.**
이 표가 없으면 없는 시험을 새로 쓰면서 이미 있는 것을 또 쓴다.

## 이 표를 읽는 법

각 기준을 세 칸으로 가른다.

| 칸 | 뜻 |
|---|---|
| **이미 재는 것** | 실재하는 시험 파일과 항. 기준 문면의 어느 부분을 무는지 함께 적는다 |
| **브라우저 잔여** | 그 시험들이 원리상 잴 수 없어 실제 브라우저가 필요한 부분 |
| **빈 자리** | 어느 계층에도 시험이 없는 부분 |

**「브라우저 잔여」와 「빈 자리」는 다르다.** 앞의 것은 이미 재어진 축을 브라우저에서 한 번 더
확인하는 일이고, 뒤의 것은 아무도 재지 않은 축을 처음 재는 일이다. 뒤의 것이 먼저다.

> ⚠️ **이 표의 「이미 재는 것」은 시험 항의 이름과 기준 문면을 대조한 결과다.**
> 그 항이 실제로 그 축을 무는지는 뮤테이션 탐침으로만 확정된다. 2026-08-28 에
> `FR-SHELL-013` 을 12/12 로 올린 판정이 정확히 이 대조만으로 세워졌다가 뒤집혔다 —
> `readAxes()` 를 끊어도 web 534 항이 통째로 초록이었다. **이 표를 근거로 요구를 닫기 전에
> 그 기준의 대표 항 하나씩에 탐침을 돌려라.**

---

## 요약

**두 축은 서로 독립이다.** 「탐침을 지났는가」와 「브라우저 잔여가 있는가」는 각각
따로 답해야 한다 — 한 칸에 섞으면 탐침을 지났으면서 잔여도 있는 기준(12)이 어느
쪽에도 정확히 들어가지 않는다. 그래서 두 열로 나눈다.

| 기준 | 확정 상태 | 탐침이 죽인 항 | 브라우저 잔여 |
|---|---|---|---|
| 1 실제 볼트 | **증거 있음** (2026-08-24 실제 볼트) | — | 해당 없음 |
| 2 라이브 프리뷰 | **탐침 확정** | 11 | 있음 — 인라인 일곱 요소 (조건부) |
| 3 위키링크·R80 표면 | **탐침 확정** | 9 | 있음 — `[[` autocomplete 팝업 |
| 4 권한 | **탐침 확정** | 201 | 있음 — 두 계정의 화면 차이 |
| 5 MCP ACL | **탐침 확정** | 2 + 2 | 없음 |
| 6 충돌 머지 뷰 | **탐침 확정 · 브라우저 검사로 잔여까지 닫음** | 3 (+ 브라우저 5) | **없음** |
| 7 한글 IME | **브라우저 검사로 자동화** | — | 없음 (검사가 맡는다) |
| 8 업로드 | **탐침 확정** | 5 | 있음 — `DataTransfer` · clipboard |
| 9 회수 즉시성·API Key | **탐침 확정** | 1 + 24 | 없음 |
| 10 첨부 권한 | **탐침 확정** | 7 | 있음 — 깨진 이미지의 겉모습 (조건부) |
| 11 휴지통 왕복 | **빈 자리를 닫음** (탐침 확정) | 2 | 있음 — 화면에서 누르는 왕복 |
| 12 버전 | **탐침 확정 · 브라우저 검사로 잔여까지 닫음** | 2 + 4 + 3 (+ 브라우저 2) | **없음** |
| 13 재조정·설치 | **탐침 확정** | 49 | 없음 |

**열셋 모두 탐침을 지났다** (2026-08-29). 「이름 대조까지」였던 여섯도 각각의 축을
겨눈 탐침이 물어 확정됐다 — 시험 항의 이름이 기준 문면과 닮았다는 것과 그 항이 그
축을 실제로 무는 것은 다르고, 이제 뒤의 것이 재어졌다.

**탐침 다섯 기준은 전부 server 전체 스위트로 쟀다** — 겨눈 파일만 돌리면 다른 곳에서
죽는 것이 보이지 않고, 그러면 「이 축을 이 시험이 무는가」의 답이 반쪽이 된다.

> **탐침 결과는 이 세션의 실행 기록이며 독립 재현되지 않았다.** 아래 각 절의 탐침
> 표는 2026-08-29 에 이 저장소에서 실제로 돌린 출력이지만, 검증자가 같은 조작을
> 다시 돌려 확인한 것은 아니다. 인용한 코드 좌표(`dispatch.ts:99` · `:606` ·
> `save-service.ts:127-128` 등)의 실재는 독립 확인을 거쳤다.

**핵심 발견 둘.**

1. **원장이 「Playwright E2E」로 지정한 8개(2·3·4·6·8·10·11·12)의 실질은 이미 대부분
   재어져 있다.** `packages/web` 에 브라우저 검사가 0개라는 사실이 「그 8개가 하나도
   재어지지 않았다」를 뜻하지 않는다. 남은 것은 브라우저가 아니면 원리상 잴 수 없는
   좁은 축들이다.
2. **기준 11 의 「버전 이력이 보존된다」를 재는 시험이 0건이다.** 휴지통 시험 파일 넷
   어디에도 `version`·`버전` 이 나오지 않는다(실측: `grep -rniE "version|버전"
   packages/server/test/app/trash/*.test.ts` 가 0줄). 구조상으로는 보존된다 —
   `version-layout.ts` 가 버전을 `.versions/<노드ID>/` 에 **노드 ID 로** 두고 휴지통이
   노드 ID 를 보존하기 때문이다. 그러나 그 왕복을 재는 항이 없고, 영구 삭제가
   `.versions/<노드ID>/` 를 함께 걷는지도 재어지지 않았다.

---

## 기준별 대조

### 기준 1 — 실제 옵시디언 볼트로 읽기·편집 (수동)

> **초판 정정 (2026-08-29)** — 이 절은 「자동 시험의 대상이 아니고 사용자만이 볼트
> 경로를 줄 수 있다」고 적었다. **틀렸다.** 그 검증은 이미 수행돼 있었고 증거가 SRS 에
> 등록돼 있다.

**이미 재는 것**

| 무엇 | 어디에 |
|---|---|
| 검증 회차 | `docs/analysis/2026-08-25.browser-manual-verification/` |
| 볼트 | 실제 옵시디언 볼트(md 1,315 · 비-md 133) — **사본**을 써서 원본을 건드리지 않았다 |
| 증거 | `FR-EDITOR-001` VE-1 (AC-1·AC-2) |
| 판정 | 트리 색인 1,451개 · 원문 전문 일치(2,252자) · 편집이 디스크에 반영 · 되돌린 편집도 반영 |

그 회차는 9개 중 2개가 FAIL 이었고 **둘 다 해소됐다** — `FR-EDITOR-007` AC-7 은
2026-08-25 에, 저장 후 포커스 유실은 2026-08-29 브라우저 검사로 통과를 확인했다.

**남은 것**: 그 증거는 2026-08-24 시점이고 그 뒤 다섯 회차가 지났다. 같은 볼트로
다시 재려면 개인 데이터가 필요하다. 다만 그 사이 `packages/web` 브라우저 검사가
서서 문서를 만들고 열고 편집하고 저장하는 왕복은 **아무 기계에서나** 재어진다.

---

### 기준 2 — 라이브 프리뷰 (R16-a 가 정한 요소 전부)

**이미 재는 것**

| 파일 | 항 | 무는 것 |
|---|---|---|
| `packages/editor/test/live-preview.test.tsx` | AC-1~AC-12 전건 (23항) | 숨는 절반과 드러나는 절반을 요소마다 |
| `packages/editor/test/tag-decoration.test.ts` | 2항 | 태그 칩의 커서 경계 |
| `packages/editor/test/code-blocks.test.ts` · `math-blocks.test.ts` · `mermaid-blocks.test.ts` | — | 요소별 데코레이션 |
| `packages/editor/test/browser-check.mjs` | 판정 12건 | **브라우저** — Mermaid 실제 SVG 렌더 |
| `packages/editor/test/table-reveal-check.mjs` | 판정 4건 | **브라우저** — 표 왕복 (AC-7) |
| `packages/editor/test/heightmap-drift-check.mjs` | 판정 7건 | **브라우저** — 위젯 heightmap 어긋남 |
| `packages/editor/test/tag-chip-check.mjs` | 판정 2건 | **브라우저** — 칩의 계산된 스타일 (AC-10) |

**탐침 확정 (2026-08-29)**: `selection-touches.ts` 의 `selectionTouches` 를 항상
거짓으로 만들자 **11항**이 죽었다 — `live-preview.test.tsx` 의 AC-6·AC-8·AC-9
「커서를 올리면 드러난다」와 `code-blocks` · `math-blocks` · `mermaid-blocks` 의
같은 축이다. 「드러나는 절반」이 한 자리에서 보호된다.

**브라우저 잔여**: R16-a 10종 중 브라우저에서 실측된 것은 Mermaid · 표 · 태그 셋뿐이다.
헤딩 · 강조 · 목록 · 링크 · 인용 · 코드블록 · 수식 일곱은 happy-dom 판정만 있다.
다만 그 일곱은 인라인 데코레이션이라 위젯 기하가 걸리지 않는다 — 브라우저가 더 잴 것이
있는지부터 판단해야 하고, 없으면 잔여가 아니다.

**빈 자리**: 없음.

---

### 기준 3 — 위키링크 자동완성 · 백링크 · R80 표면 전부의 권한 필터

R80 이 세는 표면은 여섯이고 그중 **그래프뷰는 R17 로 Phase 2** 다(R92 가 Phase 라벨을
확정하고 R165 가 후행 Phase 조작의 처분 규약을 정한다). Phase 1 의 표면은 다섯이다.

**이미 재는 것**

| 표면 | 파일 | 항 |
|---|---|---|
| `[[` 자동완성 후보 | `packages/server/test/app/document/link-servable.test.ts` | 예약 자리 · 휴지통 · 아카이브 · tombstone 4항 |
| 백링크 | 같은 파일 | 「관문에 걸린 문서는 백링크에도 나오지 않는다」 · 「관문을 통과하는 문서의 백링크는 그대로 선다」 |
| 검색 결과 | `packages/server/test/app/document/search-service.test.ts` | `SEC-WORKSPACE-004` AC-3 2항 · AC-8 |
| 태그 목록·집계 | `packages/server/test/app/document/tag-service.test.ts` | `SEC-WORKSPACE-004` AC-5 · AC-6 · 빈 목록 |
| 아웃고잉 링크 | `packages/web/test/link-panel.test.tsx` | `SEC-WORKSPACE-005` · `SEC-WORKSPACE-006` (R80-c 로 **필터 예외**) |

자동완성의 왕복 자체는 `packages/editor/test/wiki-link-insert.test.ts` 4항과
`packages/web/test/wiki-link-wiring.test.tsx` 2항이 잰다.

**탐침 확정 (2026-08-29)**: `servable.ts` 의 `isServable` 을 항상 참으로 만들자
**9항**이 죽었다 — `link-servable.test.ts` 의 자동완성 후보 필터 넷(예약 자리 ·
휴지통 · 아카이브 · tombstone)이 그중이고, `fail-closed` · `servable-composition` ·
`accessor-metrics` 도 함께 죽어 그 관문이 표면마다 서 있음이 드러났다.

**브라우저 잔여**: `[[` 를 실제로 쳤을 때 CM6 autocomplete 팝업이 뜨고 키보드로 고를 수
있는가. 팝업은 포털·포커스에 걸려 jsdom 판정과 실제가 갈릴 수 있는 부류다.

**빈 자리**: 없음. 다만 기준 문면이 **「E2E 는 판정 시점의 R80 열거를 읽어 표면마다 단언을
만든다」**를 명령하므로, 표면 다섯을 한 자리에서 열거하는 시험이 있어야 열거가 늘 때
자동으로 걸린다. 지금은 표면마다 다른 파일에 흩어져 있어 그 성질이 없다.

---

### 기준 4 — 권한 (R14 상속+가산 · R25 완전 숨김 · R43 부속 5종)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| R14 상속+가산 | `packages/server/test/domain/acl/effective-permission.test.ts` | 19항 (합집합 · 상속 끊기 · 순서 무관 · 상방 게이트) |
| R25 완전 숨김 | `packages/server/test/domain/acl/visibility.test.ts` | 12항 (pass-through · 세 번째 상태 없음) |
| R25 응답 은닉 | `packages/server/test/http/documents-acl.test.ts` · `legacy-rest-acl.test.ts` | 권한 없음과 부재가 같은 404 |
| R43 부속 5종 | `packages/server/test/http/acl-admin-routes.test.ts` | 40항 (지표 · 프리뷰 · 일괄 회수 · 시뮬레이션 · 상속 끊김 감사) |

**탐침 확정 (2026-08-29)**: `effective-permission.ts` 의 사슬 순회를 자기 노드로만
좁혀 상속 합집합을 없애자 **201항**이 죽었다. 이 저장소에서 가장 두껍게 보호되는
축이며, ACL 라우트·MCP·REST·세션이 전부 그 위에 선다.

**브라우저 잔여**: 「**두 계정으로**」가 기준 문면이다. 지금 시험은 요청자를 값으로 바꿔
서비스·라우트를 부르지, 두 브라우저 세션의 화면이 실제로 다르게 그려지는가는 재지 않는다.

**빈 자리**: 없음.

---

### 기준 5 — MCP 두 사용자 ACL · 보기 권한자의 쓰기 거부 (통합 테스트)

**이미 재는 것**

| 파일 | 항 |
|---|---|
| `packages/server/test/http/mcp-acl.test.ts` | AC-1 「서로 다른 ACL 을 가진 둘이 같은 도구를 같은 인자로 불러 각자의 것만 받는다」 · AC-1 「보기 권한만 가진 사람의 쓰기 호출은 거부된다」 · 12항 전체 |
| `packages/server/test/http/mcp-auth.test.ts` | PAT 인증 8항 |

기준 문면의 두 문장에 **정확히 대응하는 항이 각각 있다.** 검증 방법도 원장이 「통합
테스트」로 지정했고 이 시험이 그 계층이다.

**브라우저 잔여**: 없음 — MCP 는 브라우저를 지나지 않는다.

**빈 자리**: 없음. **탐침으로 확정했다 (2026-08-29).** 세 번 돌렸고 server 전체 스위트로 쟀다.

| 무엇을 겨눴나 | 죽은 항 |
|---|---|
| `dispatch.ts` `listOneLevel` 의 `visibleChildrenOf` → 무필터 `children` | AC-1 「둘이 각자의 것만 받는다」 · AC-4 「걸러진 사실 미노출」 |
| `dispatch.ts` `allowedToWrite` → 상수 참 (**바깥 겹**) | 「read-only 토큰은 편집 권한이 있어도 쓰지 못한다」 **하나뿐** |
| 위에 더해 `save-service.ts` 의 편집 판정 제거 (**안쪽 겹**) | AC-1 「보기 권한만 가진 사람의 쓰기 호출은 거부된다」 · AC-3 |

**첫 탐침은 빗나갔고 그 사실을 남긴다.** 처음에 겨눈 것은 경로 해석 관문
(`dispatch.ts:99` 의 `permissionOf`)이었는데, 그것을 열자 `SEC-ACL-006` AC-5 항 하나만
죽었다. 기준 5 를 무는 항은 목록 항목별 필터를 보므로 겨눈 자리가 그 축이 아니었다 —
**시험이 공허했던 것이 아니라 탐침이 빗나간 것이다.** 둘을 혼동하면 멀쩡한 시험을
공허하다고 판정한다.

> **부수 발견 — `SEC-ARCH-003` 의 「실행 자체를 막는다」가 회귀에서 보호되지 않는다.**
>
> 그 요구는 *"실행 자체를 막는다 — 저장을 시도한 뒤 결과를 버리는 것으로는 부족하다.
> 시도가 남기는 것이 생긴다"* 이고, 구현에서 그 역할을 하는 것이 바깥 겹
> `allowedToWrite` 다(`dispatch.ts:606`). **그 겹을 통째로 열어도 편집 권한 없는
> 사용자에 대한 항은 하나도 죽지 않는다** — 안쪽 `saveDocument` 가 같은 거부를 내기
> 때문이다. 결과가 같으니 시험이 갈리지 않는다.
>
> 즉 이 저장소는 지금 「거부된다」를 재고 「시도조차 없다」를 재지 않는다. 바깥 겹이
> 사라져도 회귀가 초록이므로, 다음 변경이 그것을 지워도 아무도 모른다.
>
> **기준 5 의 문면 밖이다** — 기준 5 는 「거부된다」까지만 요구한다. 그러나
> `SEC-ARCH-003` 의 문면 안이므로 그 요구를 닫을 때 함께 다뤄야 한다.

---

### 기준 6 — 충돌 시 머지 뷰

**이미 재는 것**

| 층 | 파일 | 항 |
|---|---|---|
| 판정 | `packages/server/test/app/document/save-service.test.ts` | AC-1~AC-5 · 「충돌 결과가 서버의 현재 본문을 함께 준다 — 머지 뷰가 그것 없이는 열리지 않는다」 |
| 상태 | `packages/web/test/autosave.test.ts` · `autosave-wiring.test.tsx` | 충돌 정지 상태기계 |
| 화면 | `packages/web/test/document-surface.test.tsx` | `FR-EDITOR-008` AC-2 · AC-3 「충돌이면 병합 화면이 뜨고 양쪽을 대조한다」 · 「저장된 상태에서는 배너도 병합도 뜨지 않는다」 |

**탐침 확정 (2026-08-29)**: `save-service.ts` 의 해시 비교를 무력화하자 **3항**이
죽었다 — `FR-STORAGE-002` AC-3·AC-4 와 「충돌 결과가 서버의 현재 본문을 함께
준다」다. 마지막 항이 중요하다: 머지 뷰가 열리려면 그 본문이 함께 와야 한다.

**브라우저 잔여**: **없음 — 2026-08-30 에 닫았다.**
`packages/web/test/merge-view-check.mjs` 가 브라우저 컨텍스트 둘로 같은 문서를 열어
차례로 저장하고, 뒤엣것에 뜬 병합 화면에서 `.cm-mergeView` 의 기하 · 두 편집기의 본문 ·
`.cm-changedLine` 을 잰다. 증거 `FR-EDITOR-008` VE-3 (AC-2·AC-3·AC-4).

**그 잔여가 실재했음이 탐침으로 확정됐다.** `MergeView.tsx` 의 `CodeMirrorMergeView`
생성을 막자 이 검사 **5항**이 죽었는데 **web 536항은 전건 초록이었다** — 머지 뷰를 아예
세우지 않아도 jsdom 계층은 하나도 알아채지 못한다. 원인은 `MergeView` 가 원문을
`pre` 로도 남기는 데 있고(가상 스크롤이라 화면 밖 줄이 렌더되지 않기 때문이며 그 배려
자체는 옳다), 그 사본 때문에 「나란히 대조한다」가 부품 없이도 참이 된다.

두 세션 축도 함께 쟀다 — `save-service.ts` 의 충돌 판정을 막자 이 검사의 「두 세션이
저장하면 충돌한다」가 죽었고 **server 는 4항**이 죽었다. 위 문단이 3항이라 적은 것은
같은 자리를 다르게 겨눈 앞 회차의 실측이며, 이번에는 `낡은 해시로 저장하면 409` 가
함께 죽어 하나 더 넓다.

**빈 자리**: 없음.

---

### 기준 7 — 한글 IME

> **초판 정정 (2026-08-29)** — 이 절은 「원장이 자동화를 배제했으므로 시험을 쓰는 것
> 자체가 기준을 바꾸는 일」이라고 적었다. **자동화가 곤란한 것은 OS 입력기를 띄우는
> 일이지 조합 단계를 내는 일이 아니었다.**

**이미 재는 것**

| 파일 | 판정 |
|---|---|
| `packages/web/test/ime-composition-check.mjs` | 조합한 글자가 정확히 한 번 들어간다 · 낱자모가 남지 않는다 · 저장까지 간다 (3/3) |
| `CON-ARCH-006` VE-3 | 2026-08-24 회차의 같은 판정 |

조합은 CDP `Input.imeSetComposition` 으로 낸다 — 낱자 → 중간 → 완성 단계를 그대로
재현하므로 OS 입력기 없이도 그 축이 재어진다. 2026-08-24 회차는 실제 볼트를 써서
그 기계에서만 재현됐고, 이 검사는 자기 문서를 만들어 아무 기계에서나 돈다.

**브라우저 잔여**: 없음 — 이 검사 자체가 브라우저다.

---

### 기준 8 — 업로드 (드래그앤드롭 · 붙여넣기 · `.res` 해시 · 크기 제한)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| 붙여넣기 | `packages/web/test/paste-upload.test.tsx` | AC-1 · AC-2 · AC-4 · 6항 |
| 트리 드롭 | `packages/web/test/upload-wiring.test.tsx` · `attachment-surface.test.tsx` | 드롭 요청 · 트리 갱신 · 드롭 가능 자리 판정 |
| `.res` 해시 저장 | `packages/server/test/app/attachment/attachment-service.test.ts` | AC-1~AC-3 「워크스페이스 루트의 `.res` 아래 해시 이름으로, 앞 2글자를 서브디렉토리로」 · 「저장된 이름에 원본 파일명이 남지 않는다」 |
| 크기 제한 | 같은 파일 | AC-1~AC-3 (제한은 설정에서 · 초과분 거부 · 파일 안 생김) |

**탐침 확정 (2026-08-29)**: `uploadLimitBytes` 를 무한대로 만들자 **5항**이 죽었다 —
`FR-ATTACH-006` AC-1·AC-2, 디렉토리 업로드와 새 버전 올리기의 같은 제한, 그리고
「크기 초과 업로드는 실체도 인덱스도 남기지 않는다」다.

**브라우저 잔여**: 실제 `DataTransfer` 드래그와 clipboard `paste` 이벤트. jsdom 시험은
이벤트를 합성해 넣는다 — 브라우저의 진짜 드롭이 같은 경로로 가는지는 재지 않는다.

**빈 자리**: 없음.

---

### 기준 9 — 권한 회수 즉시성 · 전역 API Key 불성립 (통합 테스트)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| 전역 API Key (R58) | `packages/server/test/http/legacy-credentials.test.ts` | AC-4 3항 (`X-API-Key` 는 값이 유효해도 자격이 아니다) · AC-5 2항 (재발급 안내) |
| 회수 즉시성 (R59) | `packages/server/test/app/acl/bulk-revoke.test.ts` | AC-5 「회수 직후의 판정부터 반영된다」 · AC-3 「새 사용자가 들어오거나 계정이 활성화돼도 걷힌 항목이 돌아오지 않는다」 |
| 같은 축, 다른 표면 | `tag-service.test.ts` AC-6 · `attachment-service.test.ts` `SEC-ATTACH-003` AC-2 · `trash.test.ts` AC-4 | 「권한을 잃으면 다음 조회부터」가 표면마다 |

**브라우저 잔여**: 없음 — 둘 다 요청·응답 축이다.

**빈 자리**: 없음. **탐침으로 확정했다 (2026-08-29).**

| 무엇을 겨눴나 | 죽은 항 |
|---|---|
| `mcp.ts` `subjectOf` 가 `X-API-Key` 를 자격으로 받게 | AC-4 「`X-API-Key` 는 값이 유효한 토큰이어도 자격으로 읽히지 않는다」 |
| `permission-service.ts` `permissionOf` 결과를 캐시 (즉시성 파괴) | **24항** — `bulk-revoke` AC-5 · `auth-gaps` AC-4·AC-5 · `uncovered-acs` · `attachment` `SEC-ATTACH-003` AC-2 · `trash` AC-4 · `CON-ACL-001` AC-2 외 |

첫 탐침에서 **다른 두 AC-4 항은 죽지 않았다.** 그것들은 임의의 값을 보내는 경우를
재는데 임의 값은 유효 토큰이 아니라 여전히 401 이기 때문이다 — 세 항이 서로 다른
경우를 나눠 맡고 있고 그 나눔이 정확하다.

> **초판이 놓친 항** — `packages/server/test/app/auth/auth-gaps.test.ts` 의
> 「AC-4: 권한을 회수하면 같은 PAT 로 보낸 다음 요청부터 거부된다」가 기준 9 첫
> 문장의 문면 그대로다. 초판은 `bulk-revoke.test.ts` 만 들었다. **탐침이 대조보다
> 넓게 찾는다** — 이름으로 고른 후보에는 없던 항이 그 축을 함께 물고 있었다.

---

### 기준 10 — 첨부 권한 (비공개 위치 이동 후 URL 거부 · 공개 복귀 시 즉시 열림)

**이미 재는 것**

| 파일 | 항 |
|---|---|
| `packages/server/test/app/attachment/attachment-service.test.ts` | AC-3 「소유 문서를 비공개 위치로 옮기면 기존 URL 이 거부된다」 · AC-4 「다시 열리면 첨부도 곧바로 열린다 — 캐시가 판정을 붙들지 않는다」 · AC-6 「거부된 첨부는 바이트를 주지 않는다」 |

**기준 문면의 두 문장에 정확히 대응하는 항이 각각 있다.** 이름까지 같다.

**탐침 확정 (2026-08-29)**: `openAttachment` 의 소유 문서 권한 판정을 개방하자
**7항**이 죽었다 — 이 기준의 문면 그대로인 AC-3(비공개 이동 후 거부)과
AC-4(공개 복귀 시 즉시)가 그중이고, AC-6 과 라우트 계층도 함께 죽는다.

**브라우저 잔여**: 「본문에서 깨진 이미지로 보인다」(AC-6)의 겉모습. 이것은 계산된 스타일이
아니라 이미지 로드 실패 상태이므로 브라우저가 필요한지부터 판단해야 한다.

**빈 자리**: 없음.

---

### 기준 11 — 휴지통 왕복 (원위치 · ACL · **버전 이력** 보존 · 보존 기간 경과 자동 삭제)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| 원위치 · ACL · 노드 ID | `packages/server/test/app/trash/trash.test.ts` | AC-1 · AC-3 「권한이 있으면 원본 경로로 돌아가고 ID·ACL 이 보존된다」 |
| 이름 충돌 시 복구 | 같은 파일 | AC-6 「복구 위치에 같은 이름이 있으면 접미사가 붙고 기존 것은 그대로」 |
| 부모 체인 소실 | 같은 파일 | AC-4 · AC-5 |
| 보존 기간 자동 삭제 (R86) | 같은 파일 | AC-1 기본 30일 · AC-2 「사람의 조작 없이 사라진다」 · AC-3 · AC-4 (`0` 이면 안 사라짐) |

**빈 자리 — 이 표에서 유일하다**

> **「버전 이력이 보존된다」를 재는 시험이 0건이다.**
>
> 실측: `grep -rniE "version|버전" packages/server/test/app/trash/*.test.ts` 가 **0줄**.
> 휴지통 시험 파일 넷(`trash` · `trash-view` · `archive-sweep` · `purge-audit`) 어디에도
> 버전이 등장하지 않는다.
>
> 구조상으로는 보존된다 — `packages/server/src/domain/document/version-layout.ts` 의
> `versionDirectoryOf` 가 버전을 `.versions/<노드ID>/` 에 **노드 ID 로** 두고, 그 주석이
> *"경로로 가르면 문서를 옮기는 순간 그 문서의 버전이 사라진 것처럼 된다"* 를 사유로 적는다.
> 휴지통은 노드 ID 를 보존하므로(위 AC-1·AC-3 항) 버전은 따라온다.
>
> **그러나 구조가 그렇게 생겼다는 것과 그 왕복이 성립한다는 것은 다르다.** 재지 않으면
> 다음 변경이 그것을 조용히 깨뜨린다.
>
> **이 빈 자리는 같은 날 닫혔다** — `trash.test.ts` 의 `FR-STORAGE-006 — 복구가 버전
> 이력을 데려온다 (AC-3)` 절 세 항이고 증거는 그 요구의 VE-2 다. 탐침으로 확인했다:
> `moveToTrash` 가 `.versions/<노드ID>/` 를 걷어 가게 만들면 그중 둘이 죽고, 그
> 상태에서 **기존 server 1324 항은 하나도 죽지 않았다** — 빈 자리였다는 사실이 그
> 침묵으로 확증된다. 위 서술은 그 추가 **이전** 상태를 남긴 것이다. 함께 재어지지 않은 것이 하나 더 있다 — **영구
> 삭제가 `.versions/<노드ID>/` 를 함께 걷는가.** 걷지 않으면 지워진 문서의 본문이
> 버전 파일로 남는다.

**브라우저 잔여**: 삭제→복구를 화면에서 누르는 왕복.

**함께 드러난 요구 공백 — 영구 삭제가 버전 실체를 남긴다**

> `hardDelete`(`packages/server/src/app/trash/trash-service.ts:303`)는 다섯을 걷는다 —
> 휴지통 실체·사이드카 · 첨부(`FR-ATTACH-005`) · 벡터 인덱스(`SEC-STORAGE-007` AC-1) ·
> 휴지통 인덱스 행 · 노드와 그 ACL. **버전은 그 목록에 없다.** 실측:
> `grep -rn "VERSION_DIRECTORY" packages/server/src` 가 `version-layout.ts` 자신
> 두 줄만 돌려준다 — 어떤 정리 경로도 `.versions/<노드ID>/` 를 걷지 않는다.
>
> 그래서 영구 삭제한 문서의 **본문이 버전 파일로 디스크에 남는다.** 그 요구
> `SEC-STORAGE-003` AC-4 는 *"파일 실체·사이드카·삭제 인덱스 행이 모두 사라지고
> 복구할 수 없다"* 고 적고 Implementation Notes 는 첨부만 위임한다 — **버전을 정한
> 조항이 없다.**
>
> 벡터 인덱스를 걷는 자리의 주석이 그 사유를 *"남으면 삭제된 문서의 내용이 검색으로
> 새어 나간다"* 로 적는다. **같은 논리가 버전 파일에는 더 강하게 걸린다** — 인덱스는
> 조각과 요약을 들지만 버전은 본문 그 자체를 든다.
>
> **이것은 요구 공백이므로 여기서 고치지 않는다**(제약 C-12 — 요구 없이 코드를 고치지
> 않는다). `SEC-STORAGE-003` AC-4 를 넓힐지 별도 요구를 세울지는 SRS 축의 판정이다.

---

### 기준 12 — 버전 (편집 세션당 1회 · 상한 초과 폐기 · 복원)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| 세션당 1회 (R75-a) | `packages/server/test/app/document/version-service.test.ts` | AC-1 「한 세션에서 여러 번 저장해도 스냅샷은 하나다」 · AC-2 「세션을 새로 열어 편집하면 하나 더」 |
| 상한 초과 폐기 (R62) | 같은 파일 | AC-2 · AC-3 「초과분은 가장 오래된 것부터 사라지고 최신은 남는다」 · AC-4 (실체와 인덱스 함께) |
| 복원 (R63-a) | 같은 파일 | AC-2 「고른 버전으로 복원하면 본문이 그 내용이 된다」 · AC-4 (노드 ID 불변) · 「복원 자체가 버전으로 남는다」 |
| 복원의 경계 | `restore-recursion.test.ts` | 4항 |
| 화면 | `packages/web/test/version-history.test.tsx` | AC-1 · AC-2 · AC-3 · 9항 |

기준 문면의 **세 문장 모두에 대응 항이 있다.**

**브라우저 잔여**: **없음 — 2026-08-30 에 기준 6 과 한 자리에서 닫았다.**
같은 검사(`packages/web/test/merge-view-check.mjs`)가 문서 메뉴의 「버전 기록」에서 한
판을 골라 비교하고 그 화면의 `.cm-mergeView` 와 `.cm-changedLine` 을 잰다. 증거
`IR-STORAGE-001` VE-3 (AC-1·AC-3). AC-3 「같은 컴포넌트」는 **같은 검사가 두 화면에서
같은 선택자로 같은 부품을 찾는다**는 사실로 화면 층위에서 실측된다 — 기존 VE-1 이 보던
`data-merge` 표식은 부품이 실제로 서지 않아도 남는다. 같은 탐침에 이 기준의 판정 2항이
함께 죽었다.

**빈 자리**: 없음. **탐침으로 확정했다 (2026-08-29).** 세 문장을 각각 겨눴고 서로
영향을 주지 않도록 나눠 돌렸다.

| 무엇을 겨눴나 | 죽은 항 |
|---|---|
| `snapshotIfFirstSave` 의 「그 세션이 이미 찍었는가」 검사 제거 | AC-1 「한 세션에서 여러 번 저장해도 스냅샷은 하나다」 · 「강제 저장 사이의 자동 저장은 스냅샷을 늘리지 않는다」 |
| 상한 초과분 폐기 루프를 빈 배열로 | AC-2·AC-3 「초과분은 가장 오래된 것부터」 · AC-4 (실체·인덱스) · `DR-STORAGE-005` AC-4 (사이드카) · `restore-recursion` |
| `restoreVersion` 이 고른 본문 대신 현재 본문을 저장하게 | AC-2 「고른 버전으로 복원하면 본문이 그 내용이 된다」 · `workspace-api` AC-2 · 「복원 도중 남이 고쳤으면 덮지 않는다」 |

---

### 기준 13 — 재조정 · 설치 (통합 테스트)

**이미 재는 것**

| 축 | 파일 | 항 |
|---|---|---|
| 서버 정지 중 파일 이동 (R77·R139) | `packages/server/test/app/reconciliation/reconcile.test.ts` | `REL-STORAGE-001` AC-1 (추가된 파일 → 신규 노드) · AC-2 (사라진 파일 → tombstone) · AC-3 「위 두 경우 모두 재조정 대기열에 미해소 항목으로 기재된다」 · AC-4 · AC-5 |
| 옮겨진 파일 추적 | `packages/server/test/app/watch/relocation.test.ts` · `domain/watch/correlation.test.ts` | 상관 판정 |
| 슈퍼유저 0명 → 설치 강제 (R72) | `packages/server/test/app/auth/install.test.ts` | `SEC-AUTH-010` AC-1~AC-5 |
| 관문 | `packages/server/test/http/install-gate.test.ts` | 허용목록 넷 · 그 밖의 전 경로 차단 · 「나중에 추가된 경로라도 기본이 차단」 |
| 조립 | `packages/server/test/http/install-assembly.test.ts` · `arch/assembly.test.ts` | 진입점 도달 가능성 |

기준 문면의 두 문장에 **정확히 대응하는 항이 각각 있다.**

**브라우저 잔여**: 없음 — 원장도 「통합 테스트」로 지정했다.

**빈 자리**: 없음. **탐침으로 확정했다 (2026-08-29).** 두 축을 함께 넣었다 — 서로
다른 시험 파일을 물어 결과가 갈린다.

| 무엇을 겨눴나 | 죽은 항 |
|---|---|
| `reconcile.ts` 가 새 파일을 노드로 세우지 않게 | `finding-recording` `REL-AUDIT-001` AC-6 (**기준 문면 그대로**) · `fail-closed` `SEC-STORAGE-006` AC-3 · `migrate-content` AC-3·AC-5 외 |
| `isInstalled` 를 상수 참으로 (슈퍼유저를 세지 않게) | `install.test.ts` `SEC-AUTH-010` AC-1 (**기준 문면 그대로**) · `install-assembly` 8항 · `install-routes` 7항 · `install-console` 4항 외 |

둘을 합쳐 **49항**이 죽었다. 이 두 축은 이 저장소에서 가장 두껍게 보호되는 자리다.

---

## 이 표가 바꾸는 다음 작업의 순서

앞선 핸드오프(`docs/next/2026-08-29-phase1-final-verification.md`)는 **`packages/web`
브라우저 검사 기반**을 두 번째 작업으로 두었고, E2E 8개가 전부 거기 직렬로 매달린다고
적었다. 이 표는 그 판단을 좁힌다.

~~1. 기준 11 의 빈 자리를 먼저 닫는다.~~ **2026-08-29 완료** — `trash.test.ts` 세 항,
   증거 `FR-STORAGE-006` VE-2. 함께 잴 것으로 적었던 「영구 삭제가 `.versions/` 를
   걷는가」는 **요구 공백**으로 드러나 코드를 고치지 않고 이월했다(아래 미결 표).

~~2. 통합 3개(5·9·13)에 탐침을 돌려 닫는다.~~ **2026-08-29 완료** — 세 기준의 각
   문장을 겨눈 탐침이 전부 물었다.

~~3. 기준 12 도 같은 방식으로 닫는다.~~ **2026-08-29 완료** — 세 문장을 각각 겨눴다.

~~4. 남은 자동 검증 기준 여섯(2·3·4·6·8·10)에 탐침을 돌린다.~~ **2026-08-29 완료** —
   여섯 모두 각각의 축을 겨눈 탐침이 물었다. 그 과정에서 드러난 것이 하나 있다:
   **탐침은 대조보다 넓게 찾는다.** 기준 9 에서 이름으로 고른 후보에 없던 항이 그 축을
   함께 물고 있었고, 반대로 기준 5 의 첫 탐침은 빗나갔다. 이름 대조로 세운 「이미
   재어졌다」는 판정은 탐침을 지나야 사실이 된다.

~~5. 그 다음에야 `packages/web` 브라우저 검사를 세운다.~~ **2026-08-29 완료** —
   `_web-harness.mjs` + 검사 둘(`focus-retention-check.mjs` · `ime-composition-check.mjs`)이
   8/8 통과한다. editor 의 harness 를 그대로 재사용하고 web 에만 있는 절차만 더했으며,
   개인 볼트에 기대지 않는다.

~~6. 그 위에 잔여를 하나씩 세운다.~~ **기준 6·12 완료 (2026-08-30)** —
   `merge-view-check.mjs` 7항. 둘이 `MergeView` 한 부품을 쓰므로 검사 하나가 두 기준의
   잔여를 함께 닫았다. **이 회차가 확인한 것은 잔여의 실재다** — 부품을 세우지 않는
   탐침에 브라우저 5항이 죽는 동안 web 536항은 전건 초록이었다.

## 지금 서 있는 자리

**열셋이 모두 탐침 또는 브라우저 검사를 지났다.** 남은 것은 각 기준의 「브라우저
잔여」 칸에 모인 좁은 축들이고, 그것은 이미 재어진 축을 브라우저에서 한 번 더
확인하는 일이지 아무도 재지 않은 축을 처음 재는 일이 아니다.

| 남은 브라우저 잔여 | 어느 기준 |
|---|---|
| 인라인 일곱 요소의 실제 렌더 | 2 |
| `[[` autocomplete 팝업 | 3 |
| 두 계정의 화면 차이 | 4 |
| ~~`MergeView` 실제 렌더 · 두 세션 왕복~~ **2026-08-30 닫음** | ~~6 · 12~~ |
| `DataTransfer` 드래그 · clipboard paste | 8 |
| 깨진 이미지의 겉모습 | 10 |
| 삭제→복구를 화면에서 누르는 왕복 | 11 |

이제 그 잔여들은 `packages/web` 브라우저 검사 기반 위에 하나씩 세울 수 있다 —
기반이 없어 시작하지 못하던 상태가 아니다.

## 미결 — 사용자 결정이 필요한 것

| 무엇 | 왜 |
|---|---|
| 기준 1 의 실제 볼트 경로 | 저장소에 없는 사실이며 개인 데이터다 |
| 기준 7 의 IME 체크리스트를 세울지, 어디에 둘지 | 원장이 자동화를 명시적으로 배제했다 |
| `SEC-STORAGE-003` AC-4 가 버전 실체를 열거하지 않는 문제 | 영구 삭제한 문서의 본문이 `.versions/<노드ID>/` 에 남는다. 요구를 넓힐지 새 요구를 세울지가 SRS 판정이다 |
| 원장 §4 가 인증 축을 빠뜨린 문제 | 원장 스스로 「다음 개정에서 ① 기준을 추가할지 ② 추가하지 않을 사유를 여기 남길지 판정하라」고 적었다. 지금 그대로면 **로그인할 수 없는 빌드도 이 관문을 통과한다** |
