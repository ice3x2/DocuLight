# 통합 항목 — wave 경계를 가로지르는 것

최종 검증(§5.6)의 분모에 더해지는 항목이다. 어느 한 wave 의 scope 에도 속하지 않는다.

| id | 좌표 | 규범 문장 |
| --- | --- | --- |
| `II-01` | 순서를 정한 두 제약:15 | SEC-ARCH-002 의 ACL 필터가 SEC-STORAGE-007 이 세운 벡터 인덱스 위에서 실제로 동작한다 — 저장소 wave 와 MCP wave 를 가로지른다 |
| `II-02` | 순서를 정한 두 제약:16 | MIG-AUTH-002 가 전역 API Key 를 끊는 시점에 대체 경로인 PAT 와 MCP 가 이미 서 있다 — MCP wave 와 이행 wave 를 가로지른다 |
| `II-03` | Wave 1 이 남긴 것:25 | 모든 배선이 끝난 뒤 조립 방벽 허용목록이 전량 비고 test/arch/assembly.test.ts 가 통과한다 — 배선을 나눠 가진 세 wave 를 가로지른다 |
