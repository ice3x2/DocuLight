# App Shell

| Field | Value |
|---|---|
| Document Type | scope_srs |
| Scope | SHELL |
| Scope Name | App Shell |

## 1. Scope Overview

Describe the scope.

## 2. Scope Boundaries

### In Scope

- SRS requirements

### Out of Scope

- None

## 3. Assumptions and Constraints

- None

## 4. Requirements


### FR-SHELL-001 — 좌측 사이드바 뷰 전환 탭 세 가지

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | left-sidebar, navigation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

좌측 사이드바 상단에 뷰 전환 탭 세 개(문서 트리 · 검색 · 즐겨찾기)를 두고, 옵시디언 좌측 사이드바의 탭 전환 구조와 배치를 따른다. 즐겨찾기의 대상에는 문서와 디렉토리 자체가 모두 포함된다.

#### Rationale

제품 초기 질의 Q1 을 닫으며 확정된 조항이다. 이 상단 탭은 "열린 문서 탭"이 아니라 사이드바 뷰 전환 탭이다 — 열린 문서의 탭은 중앙 본문 상단에 따로 있으므로 둘을 같은 것으로 읽으면 화면이 어긋난다. 즐겨찾기 대상에 디렉토리 자체를 포함시킨 것은 트리 비대화에 대한 대응이다.

#### Acceptance Criteria

- [ ] AC-1: 좌측 사이드바 상단에 `문서 트리` · `검색` · `즐겨찾기` 세 개의 뷰 전환 탭이 표시된다.
- [ ] AC-2: 뷰 전환 탭 하나를 선택하면 좌측 사이드바 본문이 그 뷰로 교체된다.
- [ ] AC-3: 문서 노드를 즐겨찾기에 추가하면 즐겨찾기 뷰 목록에 그 문서가 나타난다.
- [ ] AC-4: 디렉토리 노드를 즐겨찾기에 추가하면 즐겨찾기 뷰 목록에 그 디렉토리가 나타난다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R3 | informed_by | 원장 조항 R3 |
| Doc | docs/spec/00.decision-log.md#R3-a | informed_by | 원장 조항 R3-a |

#### Research / Analysis

- -

#### Implementation Notes

- 초판 문면의 "옵시디언과 완전히 동일"은 감사 M2 로 정정되어, 동형 요구가 **탭 전환 구조와 배치**로 한정됐다. 트리의 *내용*은 옵시디언과 의도적으로 다르다 — md 가 아닌 파일도 트리 목록에 표시되고, 접근 가능한 워크스페이스들이 한 번에 하나씩이 아니라 나란히 표시된다(두 규칙 모두 다른 scope 소유). 옵시디언 좌측 세트를 그대로 옮기려다 이 둘을 깨뜨리지 마라.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### IR-SHELL-001 — 설정 화면은 별도 페이지가 아니라 모달 팝업이다

| Field | Value |
| --- | --- |
| Type | interface |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | settings-modal, layout |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [설정 화면 설계](./04.screen-design-settings.md) |

#### Requirement

모든 설정 화면은 별도 페이지가 아니라 모달 팝업으로 제공하며, 모달의 내부 구조는 좌측 카테고리 리스트와 우측 설정 패널의 2단이다.

#### Rationale

에디터·렌더링뿐 아니라 설정 화면까지 옵시디언과 같은 UI/UX 를 쓴다는 결정이며, 데스크탑을 기준으로 한다.

#### Acceptance Criteria

- [ ] AC-1: 설정 화면을 열면 별도 페이지로 이동하지 않고 모달 팝업이 뜬다.
- [ ] AC-2: 설정 모달은 좌측 카테고리 리스트와 우측 설정 패널로 구성된다.
- [ ] AC-3: 좌측 카테고리 리스트에서 항목을 선택하면 우측 설정 패널만 교체된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R23 | informed_by | 원장 조항 R23 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 적용 범위는 인증 후 화면까지다 — 인증 전(pre-auth) 화면인 로그인 · 가입 신청 폼 · 설치 마법사는 이 모달 구조를 따르지 않고 전체 화면이다(`AUTH` scope 소유). 이 예외는 취향이 아니라 구조에서 온다: 모달 진입점인 좌하단 기어가 셸에 속하는데 인증 전에는 셸이 없으므로, 인증 전 화면은 이 구조를 *적용해서는 안 되는 것*이 아니라 *충족할 수 없다*. ② 모바일 대응은 Phase 2 로 이관됐다 — 이 요구는 데스크탑 기준이며 좁은 화면에서의 모달 거동을 정하지 않는다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### CON-SHELL-001 — 관리 기능의 진입점은 설정 모달 하나다

| Field | Value |
| --- | --- |
| Type | constraint |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | settings-modal, admin |
| Risk | medium |
| Stability | stable |
| Verification Method | review |
| GitHub Issue | - |
| Related Docs | [설정 화면 설계](./04.screen-design-settings.md) |

#### Requirement

관리 기능은 별도의 관리자 화면을 만들지 않고 전부 설정 모달의 좌측 카테고리로 편입한다. 문서별 ACL 지정만 예외로, 설정 모달이 아니라 별도의 공유 모달에 둔다.

#### Rationale

관리 기능의 진입점을 하나로 유지하기 위한 제약이다. 진입점이 둘이면 같은 조작이 경로에 따라 갈리고, 어느 쪽이 정본인지 아무도 모르는 상태가 된다. 문서별 ACL 지정만 예외인 이유는 그것이 구글 독스식 공유 모달이라는 자기 표면을 이미 갖기 때문이다 — 문서를 보면서 그 자리에서 공유하는 동선이라 설정 모달로 끌어오면 오히려 끊긴다.

#### Acceptance Criteria

- [ ] AC-1: 관리 기능만을 위한 별도 관리자 화면이나 별도 라우트가 존재하지 않는다.
- [ ] AC-2: 관리 기능에 도달하는 경로는 설정 모달의 좌측 카테고리다.
- [ ] AC-3: 문서별 ACL 지정은 설정 모달이 아니라 별도의 공유 모달에서 수행한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R24 | informed_by | 원장 조항 R24 |
| Requirement | IR-SHELL-001 | depends_on | 설정 모달 구조 위에서만 성립하는 제약이다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 적용 범위는 인증 후 관리 기능까지다 — 인증 전 화면은 이 원칙이 지배하지 않는다(`AUTH` scope 소유). ② 다만 슈퍼유저가 사용자를 직접 등록하는 조작은 인증 전 가입 흐름의 일부처럼 보이지만 실제로는 인증 후 관리 기능이므로 이 제약이 정면으로 적용된다 — 별도 화면을 만들지 말고 설정 모달 › 인스턴스 › `사용자 관리` 안의 조작으로 둔다. 가입을 한 화면으로 뭉뚱그려 보면 이 배치를 놓친다. ③ 카테고리를 늘려야 관리 기능이 들어갈 자리가 생기는 경우, 카테고리 목록은 닫힌 전량 목록이므로 구현이 임의로 늘릴 수 없다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### IR-SHELL-002 — 설정 모달 카테고리 전량 목록과 카테고리별 표시 권한

| Field | Value |
| --- | --- |
| Type | interface |
| Target | phase-1 |
| Status | blocked |
| Priority | medium |
| Tags | settings-modal, categories, visibility |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [설정 화면 설계](./04.screen-design-settings.md) |

#### Requirement

설정 모달의 카테고리는 아래 목록으로 확정하며, 각 카테고리는 지정된 표시 대상에게만 보인다. 개인 구역은 `에디터` · `외모(테마)` · `액세스 토큰` · `계정` · `휴지통`, 워크스페이스 관리 구역은 `워크스페이스`(개명·아카이브·복원·관리자 지정) · `권한 감사`(상속 끊김 목록 · 권한 시뮬레이션 · 주체 일괄 회수) · `감사 로그`, 인스턴스 구역은 `사용자 관리` · `그룹 관리` · `가입 승인` · `전체 워크스페이스`(목록·생성) · `인스턴스 설정`이다. 설정 모달을 여는 버튼은 좌하단 기어다. 이 목록은 전량 목록이므로 설계·구현이 카테고리를 임의로 늘릴 수 없다.

#### Rationale

감사 H2·M24 를 해소한 조항이며, 이 표가 카테고리의 *전량 목록*이라는 사실 자체가 규범이다 — 조항이 스스로 그렇게 선언하므로 설계서가 카테고리를 자기 판단으로 늘릴 수 없고, 늘리려면 이 목록을 먼저 고쳐야 한다. 심의 5인이 이 점을 착수 조건으로 지목했다. 모달을 여는 버튼을 좌하단 기어로 고정한 것은 옵시디언과 같은 자리를 쓰기 위해서다. 표시 권한을 카테고리마다 명시한 이유는 관리 기능이 전부 이 모달 하나로 모이기 때문이다 — 진입점이 하나면 그 안에서 누구에게 무엇이 보이는지가 곧 권한 경계가 된다.

#### Acceptance Criteria

- [ ] AC-1: 좌하단 기어 버튼을 누르면 설정 모달이 열린다.
- [ ] AC-2: 개인 구역의 `에디터` · `외모(테마)` · `액세스 토큰` 카테고리가 전원에게 표시된다.
- [ ] AC-3: 개인 구역의 `계정` 카테고리가 전원에게 표시되고, 그 안에 비밀번호 변경과 로그아웃 두 조작이 있다.
- [ ] AC-4: 개인 구역의 `휴지통` 카테고리는 접근 가능한 워크스페이스가 1개 이상인 사용자에게 표시된다.
- [ ] AC-5: 워크스페이스 관리 구역의 `워크스페이스` · `권한 감사` · `감사 로그` 카테고리는 해당 워크스페이스에 `관리` 권한을 가진 사용자에게만 표시된다.
- [ ] AC-6: 인스턴스 구역의 `사용자 관리` · `그룹 관리` · `가입 승인` · `전체 워크스페이스` · `인스턴스 설정` 카테고리는 슈퍼유저에게만 표시된다.
- [ ] AC-7: `인스턴스 설정` 카테고리는 가입 모드 · 업로드 크기 제한 · 보관 버전 개수 · 휴지통 보존 일수 · 감사 로그 보존 기간 다섯 설정을 담는다.
- [ ] AC-8: 위 항들이 열거한 것 외의 카테고리는 설정 모달에 나타나지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R24-a | informed_by | 원장 조항 R24-a |
| Doc | docs/spec/00.decision-log.md#G8 | related_to | 미결 공백 G8 — `에디터` · `외모` 카테고리의 개별 항목 미정의 |
| Doc | docs/spec/00.decision-log.md#G10 | related_to | 미결 공백 G10 — 사용자 스키마 미결로 `계정` 카테고리의 표시 이름 자리가 비어 있다 |
| Requirement | IR-SHELL-001 | refines | 모달 구조의 좌측 카테고리 리스트를 전량 목록으로 확정한다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 이 요구는 두 미결 공백에 걸려 `blocked` 다. **`G8`** — `에디터` · `외모` 카테고리의 *개별 항목*이 정의되어 있지 않다. 근거가 있는 셋(기본 열람 모드 · 편집 하위 뷰 · 테마)만 배치할 수 있고 나머지는 자리 확보 상태이므로, 비어 있는 항목을 지어내 채우지 마라. ② **`G10`** — 사용자 스키마(이메일 필수 여부 · 표시 이름 규칙 · 본인이 자기 표시 이름을 바꿀 수 있는가)가 미결이다. **이 축이 닫히기 전까지 `계정` 카테고리는 표시 이름 자리를 비워 둔다** — 규칙이 없고 표시 이름과 로그인 ID 의 분리 여부조차 미정이라 "표시 이름을 변경한다"가 자격증명을 바꾸는 것인지 판정할 수 없다. `계정` 은 지금 비밀번호 변경과 로그아웃 둘만 갖는다. ③ 카테고리를 늘려야 할 사정이 생기면 구현이 아니라 이 요구와 원장 표를 먼저 고쳐라 — 그것이 이 조항의 규범이다. ④ 각 카테고리의 *내부* 화면은 다른 scope 가 소유한다(휴지통 목록 · 권한 감사 3종 · 감사 로그 · 인스턴스 설정 다섯 값). 이 요구가 소유하는 것은 카테고리의 존부와 표시 권한이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19|개인 구역에 `계정` 카테고리를 신설했다|비밀번호 변경과 로그아웃의 소재가 정해지며 카테고리 표가 개정됐다. 표시 이름 변경 자리는 `G10` 이 닫힐 때까지 비워 둔다 |

### FR-SHELL-002 — 문서 단위 기능은 설정 모달이 아니라 문서 헤더 메뉴에 둔다

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | document-header, menu |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

문서 단위 기능은 설정 모달이 아니라 문서 헤더 메뉴에 둔다. 이 배치 규칙이 규범이며, 헤더 메뉴에 실제로 놓인 항목의 열거는 규범이 아니라 그 규칙을 적용한 시점의 현재 목록이다.

#### Rationale

앞 문장(문서 단위 기능은 헤더 메뉴에 둔다)이 생성적 규칙이므로 뒤의 열거는 규범이 아니라 현재 목록이다. 규칙과 목록을 구분하지 않으면 목록이 닫힌 열거로 오독되어, 새 문서 단위 기능이 생길 때마다 배치가 다투어지거나 조항 개정이 필요해진다. 인스턴스·워크스페이스 단위 기능이 설정 모달 하나로 모이는 것과 대칭이며, 나누는 축은 "이 조작의 대상이 문서 하나인가"다.

#### Acceptance Criteria

- [ ] AC-1: 문서 단위로 작용하는 기능의 진입점은 설정 모달이 아니라 문서 헤더 메뉴에 배치된다.
- [ ] AC-2: 문서 헤더 메뉴에서 그 문서의 공유 모달을 열 수 있다.
- [ ] AC-3: 문서 헤더 메뉴에서 그 문서의 버전 기록을 열 수 있다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R24-b | informed_by | 원장 조항 R24-b |
| Requirement | CON-SHELL-001 | related_to | 관리 기능의 설정 모달 단일 진입점과 대칭을 이루는 배치 규칙이다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 현재 목록에는 Phase 2 항목인 `댓글 허용 여부` 도 들어 있다 — Phase 1 헤더 메뉴에는 나타나지 않는다. 목록이 늘어나는 것은 이 규칙의 정상 동작이지 조항 위반이 아니므로, 항목 수를 고정으로 단언하는 검사를 만들지 마라. ② 다만 Phase 1 시점에 헤더 메뉴가 공유·버전 기록 둘로 유지된다는 *사실*은 다른 결정의 근거로 이미 쓰이고 있다 — 덮어쓰기 진입점을 헤더 `⋯` 가 아니라 트리 컨텍스트 메뉴에 둔 판단이 "Phase 1 헤더에 자리가 없다"에 기댄다. 헤더 메뉴에 Phase 1 항목을 더 넣으려면 그 판단을 함께 재검토하라.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-003 — 트리 컨텍스트 메뉴와 트리 상단 새 노트 버튼

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | tree, context-menu |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

좌측 트리에 컨텍스트 메뉴와 트리 상단 `새 노트` 버튼을 둔다. 컨텍스트 메뉴 항목은 새 문서 · 새 디렉토리 · 이름 변경 · 이동 · 복사 · 삭제 · 공유 · 즐겨찾기 · 새 버전 올리기이며, 각 항목의 활성 여부는 파일 조작별 필요 권한 표를 따른다. `새 버전 올리기` 는 파일 노드 전용이고, `복사` 는 원본 `보기` 만으로 활성된다.

#### Rationale

감사 H1 을 해소한 조항이다 — 이것이 없으면 Phase 1 사용자는 업로드로만 문서를 만들 수 있다. 초판 열거에는 `복사` 와 `새 버전 올리기` 가 빠져 있었고 뒤에 추가됐다. 복사는 워크스페이스 경계를 넘는 유일한 수단이고 새 버전 올리기는 덮어쓰기의 유일한 합법 경로이므로, 진입점이 없으면 두 기능에 도달할 수 없는 상태였다. `복사` 가 `보기` 만으로 활성되는 이유는 복사가 원본을 건드리지 않는 비파괴 동작이기 때문이다 — 읽을 수 있으면 복사할 수 있다. 이동·삭제가 `편집` 을 요구하는 것과 이 지점에서 갈린다.

#### Acceptance Criteria

- [ ] AC-1: 트리 상단에 `새 노트` 버튼이 있고, 누르면 새 문서를 만들 수 있다.
- [ ] AC-2: 트리 노드에서 컨텍스트 메뉴를 열면 새 문서 · 새 디렉토리 · 이름 변경 · 이동 · 복사 · 삭제 · 공유 · 즐겨찾기 · 새 버전 올리기 항목이 나타난다.
- [ ] AC-3: 요청자가 그 조작에 필요한 권한을 갖지 않은 항목은 비활성으로 표시된다.
- [ ] AC-4: `새 버전 올리기` 항목은 파일 노드의 컨텍스트 메뉴에만 나타난다.
- [ ] AC-5: 디렉토리 노드와 pass-through 노드의 컨텍스트 메뉴에는 `새 버전 올리기` 항목이 없다.
- [ ] AC-6: `복사` 항목은 원본에 대한 `보기` 권한만 있으면 활성되며 `편집` 권한을 요구하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R88 | informed_by | 원장 조항 R88 |
| Doc | docs/spec/00.decision-log.md#R88-a | informed_by | 원장 조항 R88-a |
| Doc | docs/spec/00.decision-log.md#R88-b | informed_by | 원장 조항 R88-b |
| Requirement | FR-SHELL-001 | depends_on | 문서 트리 뷰 위에서 성립하는 진입점이다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 활성/비활성 판정의 정본은 파일 조작별 필요 권한 표(`ACL` scope 소유)이며 이 요구가 그 표를 복제하지 않는다 — 표가 바뀌면 메뉴의 활성 규칙도 함께 바뀐다. 여기에 권한 값을 하드코딩하면 두 곳이 조용히 어긋난다. ② 디렉토리 이동·삭제에는 추가 게이트가 걸린다: 하위에 요청자가 볼 수 없는 노드가 하나라도 있으면 거부하고 `관리` 를 요구한다(그 규칙도 다른 scope 소유). 메뉴 활성만 보고 실행이 통과한다고 가정하지 마라. ③ 이 요구가 소유하는 것은 진입점의 존부와 노드 종류별 노출이며, 각 항목이 실행하는 조작의 동작은 각자의 요구가 소유한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-004 — 우측 사이드바 신설과 세 탭

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | right-sidebar, links |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

우측 사이드바를 옵시디언과 동일한 위치·구조로 신설하고, 백링크 · 아웃고잉 링크 · 태그 세 탭을 둔다. 표시 내용에는 요청자의 유효 권한 필터를 적용한다.

#### Rationale

감사 H5 를 해소한 조항이며, 링크 계열 기능이 놓일 화면이다. 위치·구조를 옵시디언과 맞춘 것이 이 조항의 규범이고, 그 문면은 뒤에 태그 탭의 범위를 판정할 때 결정적 근거가 됐다 — 탭 묶음이 옵시디언 우측 세트의 전사임이 실측으로 확인됐기 때문이다. Phase 2 에 아웃라인·프로퍼티 탭이 추가되면 우측이 문서 문맥 넷 + 전역 하나가 되어 옵시디언 우측 세트와 같은 집합이 된다.

#### Acceptance Criteria

- [ ] AC-1: 우측 사이드바가 화면 우측에 표시되고 백링크 · 아웃고잉 링크 · 태그 세 탭을 갖는다.
- [ ] AC-2: 탭 하나를 선택하면 우측 사이드바 본문이 그 탭의 내용으로 교체된다.
- [ ] AC-3: 백링크 탭은 요청자가 볼 수 없는 문서에서 온 역참조를 표시하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R89 | informed_by | 원장 조항 R89 |
| Doc | docs/spec/00.decision-log.md#G14-a | related_to | 미결 공백 G14-a — 우측 사이드바의 갱신 축이 둘로 갈려 갱신 시점 규정이 비어 있다 |
| Requirement | SEC-WORKSPACE-005 | related_to | 아웃고잉 링크 탭이 이 요구의 권한 필터에서 빠지는 예외 규칙의 정본이다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 세 탭의 갱신 계기가 같지 않다 — 백링크·아웃고잉은 활성 문서를 추종하고 태그는 문서와 독립인 전역 색인이라 계기가 다르다. 화면 최신성 규정이 열린 공백(`G14`·`G14-a`)이고, 갱신 대상을 "트리·검색·즐겨찾기·우측 사이드바" 네 자리로만 세면 태그 축이 빠진다. 그래서 이 요구의 성숙도가 `evolving` 이다. ② 아웃고잉 링크 탭은 권한 필터의 예외다 — 본문에 있는 링크를 그대로 나열하며 미해결 링크도 표시하되 "존재하지 않음"과 "권한 없음"을 구분하지 않는다. 그 예외 규칙의 정본은 `WORKSPACE` scope 의 `SEC-WORKSPACE-005`(원장 `R80-c`)가 소유하며, 백링크에는 예외가 적용되지 않는다(남의 본문에서 유래하기 때문이다). ③ Phase 2 의 아웃라인·프로퍼티 탭은 이 요구의 범위가 아니다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### DR-SHELL-001 — 런타임 변경 가능한 설정의 단일 저장소는 DB 다

| Field | Value |
| --- | --- |
| Type | data |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | settings, storage |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

런타임에 변경 가능한 설정(가입 모드 · 업로드 제한 · 버전 개수 · 휴지통 보존 등)은 DB 를 단일 저장소로 한다. config 파일은 부트스트랩 전용이며 포트 · docsRoot · DB 경로만 담는다.

#### Rationale

감사 M12 를 해소한 조항이다. 같은 사실이 config 파일과 DB 두 곳에 있으면 재기동 시 config 값으로 조용히 원복되어, 관리자가 화면에서 바꾼 값이 사라진다. 보관 버전 개수의 저장 소재가 미결로 표기돼 있던 것도 이 조항으로 닫혔다.

#### Acceptance Criteria

- [ ] AC-1: 런타임에 변경 가능한 설정 값은 DB 에서 읽고 DB 에 쓴다.
- [ ] AC-2: config 파일은 포트 · docsRoot · DB 경로만 담고 런타임 변경 가능한 설정을 담지 않는다.
- [ ] AC-3: 설정 모달에서 런타임 설정을 변경한 뒤 서버를 재기동해도 그 값이 유지된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R96 | informed_by | 원장 조항 R96 |
| Requirement | IR-SHELL-002 | related_to | 인스턴스 설정 카테고리가 이 저장소를 읽고 쓰는 화면이다 |

#### Research / Analysis

- -

#### Implementation Notes

- 부트스트랩 전용 열거(포트 · docsRoot · DB 경로)는 자의적 목록이 아니라 "DB 를 열기 전에 알아야 하는 값"이라는 성격으로 정해진 것이다 — 새 설정을 config 파일에 넣고 싶다면 그 값이 DB 접속 이전에 필요한지를 먼저 판정하라. 그렇지 않은 값을 config 로 옮기는 순간 이 요구가 깨진다. 인스턴스 설정 카테고리에 놓이는 다섯 값(가입 모드 · 업로드 크기 제한 · 보관 버전 개수 · 휴지통 보존 일수 · 감사 로그 보존 기간)이 전부 이 요구의 적용 대상이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-005 — 상단 문서 탭

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | document-tabs |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

열린 문서를 상단 문서 탭으로 전환할 수 있게 한다. 문서 탭은 Phase 1 범위다.

#### Rationale

옵시디언과 동일한 느낌이라는 제품 정의의 핵심 구성요소이며, 그래서 Phase 2 로 미루지 않고 Phase 1 에 둔다.

#### Acceptance Criteria

- [ ] AC-1: 문서를 열면 상단 탭 스트립에 그 문서의 탭이 생긴다.
- [ ] AC-2: 탭을 클릭하면 본문 영역이 그 문서로 전환된다.
- [ ] AC-3: 탭을 닫으면 그 문서가 탭 스트립에서 사라진다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R98 | informed_by | 원장 조항 R98 |

#### Research / Analysis

- -

#### Implementation Notes

- Phase 2 의 분할 창이 도입되면 탭이 창별로 귀속되도록 지금 설계해 두어야 한다 — 옵시디언에서 탭과 분할 창은 같은 시스템이고, 탭 상태를 전역 단일 목록으로 만들어 두면 분할 도입 시 구조를 다시 짜야 한다. Phase 1 에서 창이 하나라는 사실을 자료구조에 굳히지 마라.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-006 — 노드 ID 기반 문서 딥링크

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | deep-link, routing |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

문서 딥링크(URL 라우팅)를 제공한다. URL 에는 경로가 아니라 노드 ID 를 사용하고 브라우저 뒤로·앞으로가 동작한다. 권한 없는 노드에 대한 404 통일 규칙은 `SEC-ACL-006` 이 소유하며 이 요구가 정하지 않는다.

#### Rationale

사내 메신저로 문서 링크를 공유하는 것은 사내 문서 도구의 핵심 수요다. 경로 독립 노드 ID 와 404 통일이라는 재료가 이미 갖춰져 있어 결정만 하면 되는 상태였다. 경로가 아니라 노드 ID 를 URL 에 쓰는 이유는 문서를 개명·이동해도 링크가 깨지지 않게 하기 위해서다 — 경로 기반 URL 이었다면 노드 ID 를 도입해 얻은 이점이 URL 계층에서 사라진다.

#### Acceptance Criteria

- [ ] AC-1: 문서를 열면 그 문서의 노드 ID 를 담은 URL 이 주소창에 반영된다.
- [ ] AC-2: 그 URL 로 접근한 다른 사용자에게 권한이 있으면 같은 문서가 열린다.
- [ ] AC-3: 문서를 개명하거나 이동한 뒤에도 기존 딥링크가 같은 문서를 연다.
- [ ] AC-4: 브라우저 뒤로·앞으로 버튼이 문서 이동 이력을 따라 동작한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R99 | informed_by | 원장 조항 R99 |
| Doc | docs/spec/00.decision-log.md#R99-a | informed_by | 원장 조항 R99-a |
| Requirement | SEC-ACL-006 | depends_on | 404 통일의 정본 — 그 AC-6 이 딥링크 표면을 직접 덮는다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 404 통일의 정본은 `SEC-ACL-006` 이며 그 AC-6 이 딥링크 화면 표면까지 덮는다 — 이 요구는 참조만 하고 복제하지 않는다. ② 노드 ID 가 URL 에 직접 노출되므로 순차 정수를 쓰면 URL 을 훑는 것만으로 존재 여부를 재는 열거 오라클이 되어 404 통일의 취지가 URL 계층에서 무너진다 — 추측 불가한 형식이어야 한다는 요건은 노드 ID 조항이 소유한다. ③ 딥링크 404 표면에는 권한 요청 버튼을 두지 않는다. Phase 2 에서 권한 요청 워크플로가 도입돼도 마찬가지다 — 버튼의 존재 자체가 그 자리에 대상이 있다는 사실을 드러낸다. ④ 브라우저 뒤로·앞으로는 URL 이 문서 상태를 반영하는 것과 같은 라우팅 기제의 두 면이고 scope·검증 방법이 같아 별도 요구로 분리하지 않는다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-007 — 휴지통 카테고리의 표시 조건과 전 워크스페이스 통합 목록

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | trash, settings-modal |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [설정 화면 설계](./04.screen-design-settings.md) |

#### Requirement

휴지통 카테고리는 접근 가능한 워크스페이스가 1개 이상인 사용자 전원에게 표시한다. 휴지통은 접근 가능한 전 워크스페이스의 통합 목록이며 `워크스페이스` 열과 필터를 제공한다. 열람 범위 판정과 영구 삭제 가능 여부는 행마다 그 행의 워크스페이스를 기준으로 적용하므로, 그 워크스페이스의 관리자는 전체/본인분 범위 토글을 갖고 그 밖의 사용자는 본인 삭제분으로 고정된다.

#### Rationale

삭제 권한은 `편집` 에게 열려 있는데 휴지통이 `관리` 전용 구역에 있어 편집자가 자기 문서를 지우고도 복구할 수 없는 상태였다. 표시 조건만 넓히고 데이터 범위는 기존 휴지통 규칙으로 분기시켜 그 모순을 닫았으며, 관리 기능 단일 진입점 원칙은 유지된다 — 휴지통은 여전히 설정 모달 안에 있고 구역만 개인으로 옮겼다. 통합 목록으로 만든 이유는 삭제 격리 디렉토리가 워크스페이스마다 있는데 휴지통이 개인 구역으로 이동해 워크스페이스 스코프 선택기를 쓸 수 없게 됐기 때문이다. 통합 목록 + 워크스페이스 열·필터라는 형태는 검색 탭의 범위 선택기와 같은 선례를 따른다.

#### Acceptance Criteria

- [ ] AC-1: 접근 가능한 워크스페이스가 1개 이상인 사용자에게 휴지통 카테고리가 표시된다.
- [ ] AC-2: 접근 가능한 워크스페이스가 하나도 없는 사용자에게는 휴지통 카테고리가 표시되지 않는다.
- [ ] AC-3: 휴지통 목록은 접근 가능한 전 워크스페이스의 항목을 하나의 목록으로 보여주고 `워크스페이스` 열을 갖는다.
- [ ] AC-4: `워크스페이스` 필터로 목록을 특정 워크스페이스의 항목으로 좁힐 수 있다.
- [ ] AC-5: 어떤 워크스페이스에 `관리` 권한을 가진 사용자는 그 워크스페이스 항목에 대해 전체/본인분 범위 토글을 쓸 수 있다.
- [ ] AC-6: `관리` 권한이 없는 워크스페이스의 항목은 요청자 본인이 삭제한 것만 목록에 나타난다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R100 | informed_by | 원장 조항 R100 |
| Doc | docs/spec/00.decision-log.md#R100-a | informed_by | 원장 조항 R100-a |
| Requirement | IR-SHELL-002 | refines | 카테고리 표의 휴지통 행이 정한 표시 조건을 전개한다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 행 단위 판정이 이 요구의 핵심이다 — 한 사용자가 WS-A 의 관리자이면서 WS-B 의 편집자이면, 같은 목록 안에서 A 의 항목은 전체가 보이고 B 의 항목은 본인 삭제분만 보인다. 사용자 단위로 한 번 판정해 목록 전체에 적용하면 이 조항이 깨진다. ② 열람 범위 규칙(본인 삭제분 + 워크스페이스 관리자는 전체)과 영구 삭제의 필요 권한(`관리`)의 정본은 이 요구가 아니라 각각 휴지통 규칙 조항과 파일 조작 권한 표가 소유한다 — 다른 scope 이며 여기에 값을 복제하지 마라. ③ 보존 기간 경과 시의 자동 영구 삭제도 이 요구의 범위가 아니다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### SEC-SHELL-001 — 휴지통 영구 삭제 버튼은 권한이 없으면 표시하지 않는다

| Field | Value |
| --- | --- |
| Type | security |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | trash, visibility |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [설정 화면 설계](./04.screen-design-settings.md) |

#### Requirement

휴지통의 영구 삭제 버튼은 그 조작 권한이 없는 요청자에게 표시하지 않는다.

#### Rationale

휴지통 카테고리가 `관리` 전용 구역에 있던 동안에는 카테고리 자체가 보이지 않는 것에 기댈 수 있었다. 카테고리가 전원에게 보이게 되면서 그 방어가 사라졌으므로 버튼 수준의 숨김이 필요해졌다.

#### Acceptance Criteria

- [ ] AC-1: 영구 삭제 권한이 없는 요청자의 휴지통 화면에는 영구 삭제 버튼이 나타나지 않는다.
- [ ] AC-2: 영구 삭제 권한이 있는 요청자의 휴지통 화면에는 영구 삭제 버튼이 나타난다.
- [ ] AC-3: 권한 판정은 항목이 속한 워크스페이스를 기준으로 하므로, 한 목록 안에 버튼이 있는 항목과 없는 항목이 함께 나타날 수 있다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R100-b | informed_by | 원장 조항 R100-b |
| Requirement | FR-SHELL-007 | depends_on | 휴지통이 전원에게 보이게 된 결과로 필요해진 숨김이다 |

#### Research / Analysis

- -

#### Implementation Notes

- 비활성으로 노출하는 것과 표시하지 않는 것은 다르며, 이 조항이 요구하는 것은 미표시다 — 트리 컨텍스트 메뉴가 권한 없는 항목을 비활성으로 두는 것과 이 지점에서 갈리므로, 두 표면에 같은 공통 컴포넌트를 쓴다면 노출 정책을 인자로 받아야 한다. 영구 삭제에 필요한 권한 값 자체는 파일 조작 권한 표(`ACL` scope 소유)가 정한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### SEC-SHELL-002 — 이름 충돌 처리는 가시 여부와 무관하게 하나로 통일한다

| Field | Value |
| --- | --- |
| Type | security |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | name-collision, oracle |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

이름 충돌은 대상이 요청자에게 보이든 보이지 않든 항상 같은 방식으로 처리한다 — 자동 접미사를 붙이고 동일한 안내를 표시한다. 확인 단계를 두지 않는다.

#### Rationale

개정 전에는 보이는 충돌은 거부하고 대안을 제안했고, 보이지 않는 충돌은 조용히 자동 리네임했다. 흐름의 차이 자체가 "거기에 무언가 있다"를 알려주는 존재 오라클이었다 — 완전 숨김 원칙 아래에서는 두 경로가 관측상 구별되지 않아야 한다. 확인 단계를 없앤 것도 같은 이유다: 확인이 뜨는지 안 뜨는지가 그대로 오라클이 된다.

#### Acceptance Criteria

- [ ] AC-1: 요청자에게 보이는 파일과 이름이 충돌하면 자동 접미사가 붙은 이름으로 생성된다.
- [ ] AC-2: 요청자에게 보이지 않는 파일과 이름이 충돌해도 자동 접미사가 붙은 이름으로 생성된다.
- [ ] AC-3: 두 경우의 안내 문구가 동일하다.
- [ ] AC-4: 이름 충돌 시 사용자에게 확인을 묻는 단계가 없다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R102 | informed_by | 원장 조항 R102 |

#### Research / Analysis

- -

#### Implementation Notes

- 이 조항은 기존의 가시 충돌 처리 규칙(거부 + 대안 제안)을 개정한 것이다 — 옛 문언을 따르는 구현·설계가 남아 있으면 함께 고쳐야 하며, 두 경로가 남아 있는 상태 자체가 결함이다. 접미사의 형식은 원장이 정하지 않았으므로 이 축은 구현 판단이지만, 형식이 충돌 대상의 가시 여부에 따라 달라져서는 안 된다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-008 — 덮어쓰기의 유일한 경로는 파일 노드 전용 새 버전 올리기다

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | upload, versioning, context-menu |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

기존 파일 덮어쓰기는 "기존 파일을 선택해 새 버전 올리기"라는 별도 경로로만 제공한다. 그 진입점은 트리 컨텍스트 메뉴의 파일 노드 전용 항목 `새 버전 올리기…` 이며 디렉토리·pass-through 노드에는 나타나지 않는다. 필요 권한은 대상 파일 `편집` 이다.

#### Rationale

덮어쓰기 경로를 이 하나로 좁히면 존재 오라클이 생기지 않는다 — 이 경로는 요청자에게 보이는 파일에만 존재하므로 "덮어쓸 수 있는가"라는 물음이 보이지 않는 파일에 대해 제기되지 않는다. 이름 충돌을 항상 자동 접미사로 통일한 결정과 짝을 이루며, 둘을 합치면 덮어쓰기 의도는 명시적 경로로만 표현된다. 진입점을 문서 헤더 메뉴가 아니라 트리 컨텍스트 메뉴에 둔 이유는 Phase 1 헤더 메뉴가 공유·버전 기록 둘로 한정되어 자리가 없기 때문이다. md 와 바이너리를 다르게 다루는 것은 md 만 버전 보관 대상이어서 되돌릴 수 있고 바이너리는 그렇지 않기 때문이다.

#### Acceptance Criteria

- [ ] AC-1: 업로드·생성 흐름에는 기존 파일을 덮어쓰는 선택지가 없다.
- [ ] AC-2: 파일 노드의 컨텍스트 메뉴에서 `새 버전 올리기…` 를 실행해 그 파일을 덮어쓸 수 있다.
- [ ] AC-3: 대상 파일에 `편집` 권한이 없으면 `새 버전 올리기…` 를 실행할 수 없다.
- [ ] AC-4: md 문서에 새 버전을 올리면 이전 본문이 버전으로 보관된다.
- [ ] AC-5: 바이너리 파일에 새 버전을 올리면 복구할 수 없다는 경고를 표시한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R102-a | informed_by | 원장 조항 R102-a |
| Doc | docs/spec/00.decision-log.md#R102-b | informed_by | 원장 조항 R102-b |
| Requirement | FR-SHELL-003 | depends_on | 진입점이 트리 컨텍스트 메뉴에 있다 |
| Requirement | SEC-SHELL-002 | related_to | 이름 충돌을 자동 접미사로 통일한 결정과 짝을 이루는 덮어쓰기 경로다 |
| Requirement | FR-SHELL-002 | related_to | 헤더 메뉴에 자리가 없다는 사실이 이 배치의 근거다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 헤더 메뉴가 Phase 1 에서 둘로 유지되는 것은 문서 헤더 메뉴 배치 규칙의 *귀결*이지 그 자체가 규범이 아니다 — 그 목록은 생성적 규칙의 현재 상태이고 Phase 2 에 `댓글 허용 여부` 가 들어온다. 즉 이 진입점 배치의 근거는 "헤더 메뉴가 영원히 둘이다"가 아니라 "Phase 1 시점에 자리가 없다"이다. ② 바이너리 경고의 문구와 형태는 원장이 정하지 않았다. ③ 버전 생성의 단위·상한 규칙은 다른 scope 가 소유하며 이 요구가 그 값을 정하지 않는다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### SEC-SHELL-003 — 디렉토리 복사는 요청자에게 보이는 노드만 복사한다

| Field | Value |
| --- | --- |
| Type | security |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | copy, oracle, visibility |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

디렉토리를 복사할 때는 요청자에게 보이는 노드만 복사하고 누락된 노드가 있다는 사실을 알리지 않는다. 복사 다이얼로그에는 숨은 노드의 유무와 무관하게 항상 동일한 문구를 표시한다. 복사가 끝나면 복사된 항목 수를 토스트로 알린다.

#### Rationale

UX 패널 4:0. 남은 두 선택지가 모두 깨진다 — 전부 복사하면 복사본이 대상 부모에서 상속해 권한 상승이 되고, 숨은 노드가 있다고 거부하면 그 거부 자체가 존재 오라클이 된다. 보이는 것만 복사하는 쪽이 성립하는 이유는 복사가 원본을 건드리지 않는 비파괴 동작이기 때문이다. 안내 문구를 조건 없이 항상 같게 두는 이유는 조건부 표시가 그 자체로 오라클이기 때문이다. 복사된 항목 수를 알리는 것은 노출이 되지 않는다 — 복사된 것은 언제나 요청자가 이미 보던 것뿐이라 새로 알려주는 사실이 없고, 대량 이관 시 사후 대조점이 된다.

#### Acceptance Criteria

- [ ] AC-1: 요청자가 볼 수 없는 하위 노드는 복사 결과에 포함되지 않는다.
- [ ] AC-2: 복사 결과에 누락된 노드가 있어도 그 사실을 알리는 표시가 나타나지 않는다.
- [ ] AC-3: 복사 다이얼로그는 숨은 노드의 유무와 관계없이 "권한에 따라 일부 항목이 제외될 수 있습니다"라는 동일한 문구를 표시한다.
- [ ] AC-4: 복사가 끝나면 복사된 항목 수를 담은 토스트가 표시된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R105 | informed_by | 원장 조항 R105 |
| Doc | docs/spec/00.decision-log.md#R105-a | informed_by | 원장 조항 R105-a |
| Doc | docs/spec/00.decision-log.md#R105-b | informed_by | 원장 조항 R105-b |
| Requirement | FR-SHELL-003 | depends_on | 복사의 진입점이 트리 컨텍스트 메뉴에 있다 |

#### Research / Analysis

- -

#### Implementation Notes

- 토스트의 수치는 요청자가 볼 수 있던 노드의 수이지 원본 하위의 전체 노드 수가 아니다 — 후자를 세거나 "N개 중 M개"처럼 분모를 함께 보이면 그 차이가 곧 숨은 노드의 개수가 되어 이 요구가 무너진다. 같은 이유로 "일부가 제외됨" 같은 조건부 후행 문구도 붙이지 마라.
- [2026-08-19] AC-3 이 문구를 문자열로 못박은 것은 원장 `R105-a` 가 예시 표기 없이 그 문면을 직접 정했기 때문이다. 같은 문서의 `FR-SHELL-009` AC-6 이 문자열을 못박지 않는 것은 그 근거인 `R106-c` 가 문구를 「예:」 로 들었기 때문이며, 두 자리의 처리 차이는 원장의 처리 차이를 그대로 따른 결과다 — 일관성을 이유로 이 문자열을 지우지 마라.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-009 — 태그 탭은 전 워크스페이스 태그를 출현 문서 수로 집계한다

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | high |
| Tags | tag-panel, right-sidebar, acl-filter |
| Risk | medium |
| Stability | evolving |
| Verification Method | Playwright E2E |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

우측 사이드바 `태그` 탭은 접근 가능한 전 워크스페이스의 태그 목록과 각 태그의 출현 문서 수를 보여주며 범위 선택기를 제공한다. 집계 원천에 요청자의 유효 권한 필터를 적용한다 — 개수만 거르는 것이 아니라 태그의 등장 자체가 필터를 거치므로, 요청자가 볼 수 있는 문서에 한 번도 나타나지 않는 태그는 목록에 없다. 개수의 단위는 `출현 문서 수` 이며 `출현 횟수` 가 아니다. 개수 옆에는 조건 없이 항상 같은 기준 문구를 상시 표기한다.

#### Rationale

심의 3인 만장일치. 초판 규정은 "현재 열린 문서의 태그"였고 UX 패널 3:1 로 통과했으나, 2026-08-18 옵시디언 실측 앞에서 그 다수 의견의 근거 두 개가 모두 무너져 전복됐다. 뒤집기의 근거는 셋이다 — ① 우측 사이드바 조항의 문면이 "옵시디언과 동일 위치·구조"를 요구하고, 그 탭 묶음이 옵시디언 우측 세트의 전사임이 실측으로 확인됐다 ② 그 조항 근거 칸의 "권한 필터 적용"이 초판 아래에서 무대상이었다 — 현재 문서의 태그는 이미 볼 수 있는 문서의 것이라 걸러질 대상이 원리적으로 0 이며, 조항이 자기 화면에 대해 공허해지는 것은 그 조항이 다른 화면을 상정했다는 흔적이다 ③ 태그 어휘를 열거하는 자리가 Phase 1 제품 전체에 없어, 태그 탐색의 유일한 시작점이 "이미 열어 둔 문서에 그 태그가 달려 있는 경우"라 태그 체계가 자기 자신을 통해서만 발견되는 닫힌 고리였다. **전복의 근거로 "옵시디언과 동일한 UI"라는 제품 정의를 인용하지 않았다** — 심의 3인이 각자 그 동형 요구는 `에디터와 렌더링 결과` 둘로 한정되어 사이드바 패널의 내용 범위에 미치지 않는다고 판정했다. 개수의 단위를 `출현 문서 수` 로 정하고 옵시디언 문구(`number of occurrences`)를 그대로 옮기지 않은 것은 의도적이다 — 권한 필터가 문서 단위로 걸리므로 출현 횟수는 이 필터 위에서 정의되지 않는다. 개수 옆 문구를 조건 없이 상시 표기하는 것은 조건부 표시가 그 자체로 오라클이기 때문이며, 개수가 사람마다 달라지는 것은 권한 필터의 정상 동작이지 결함이 아니다. 범위 선택기의 선례는 휴지통 통합 목록과 검색 탭이다.

#### Acceptance Criteria

- [ ] AC-1: `태그` 탭은 접근 가능한 전 워크스페이스의 태그를 하나의 목록으로 보여준다.
- [ ] AC-2: 범위 선택기로 목록을 특정 워크스페이스로 좁힐 수 있다.
- [ ] AC-3: 요청자가 볼 수 있는 문서에 한 번도 나타나지 않는 태그는 목록에 나타나지 않는다.
- [ ] AC-4: 각 태그 옆의 수치는 그 태그가 달린 문서 중 요청자가 볼 수 있는 문서의 개수다.
- [ ] AC-5: 한 문서에 같은 태그가 여러 번 나타나도 그 문서는 수치에 1 로 계산된다.
- [ ] AC-6: 개수 옆의 기준 문구는 숨은 항목의 유무와 관계없이 항상 같은 문구로 표시된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R106 | informed_by | 원장 조항 R106 |
| Doc | docs/spec/00.decision-log.md#R106-c | informed_by | 원장 조항 R106-c |
| Doc | docs/spec/00.decision-log.md#G31 | related_to | 미결 공백 G31 — 본문 `#태그` 의 렌더·클릭 규정 부재. 이 조항의 전복으로 곧바로 드러난 공백이다 |
| Doc | docs/spec/00.decision-log.md#G14-a | related_to | 미결 공백 G14-a — 전역 색인인 이 탭의 갱신 계기가 같은 사이드바의 다른 두 탭과 다르다 |
| Requirement | FR-SHELL-004 | refines | 우측 사이드바 세 탭 중 태그 탭의 내용을 정한다 |

#### Research / Analysis

- 옵시디언 1.8.9 설치본의 `app.js` · `app.css` · `i18n/en.json` 과 실제 볼트 설정을 원자료로 직접 읽어 실측했고, 독립 검증자가 10개 주장 전건을 원자료에서 재현했다. 확인된 사실 둘 — **위치는 우측**이다(패널 초기화가 `right` 를 기본값으로 쓰고 모바일에서만 `left` 로 바뀌며, 기본 활성 상태다). **범위는 전역**이다(en.json 의 설명 문구가 *"Displays all your tags along with their number of occurrences"* 이고, 구현이 메타데이터 캐시의 태그 조회로 파일 캐시 전체를 순회하며 활성 파일 필터가 없다. 초기 정렬값은 `frequency` 다). 즉 위치는 초판과 일치했고 범위는 불일치했다.

#### Implementation Notes

- ① **집계 원천에 필터를 적용해야 한다.** 전체를 집계한 뒤 개수만 깎으면, 요청자가 볼 수 없는 문서에만 있는 태그가 0 건으로 목록에 남아 그 태그의 존재가 드러난다. 목록에서 태그 자체가 빠져야 한다. ② 본문의 `#태그` 가 어떻게 렌더되고 클릭 가능한지가 어느 조항에도 없다(`G31`) — 라이브 프리뷰 대상 정본 목록에 태그가 없어 의도적 배제인지 누락인지 먼저 판정해야 한다. 이 탭의 집계 자체는 그 판정과 독립으로 성립하지만, 태그 진입점이 이 패널 하나로 줄어드는 상태가 그 공백의 증상이다. ③ 이 탭의 갱신 계기는 같은 사이드바의 백링크·아웃고잉과 다르다 — 그 둘은 활성 문서를 추종하고 이 탭은 문서와 독립인 전역 색인이다(`G14-a`). ④ 검증 방법이 `Playwright E2E` 이고 우선순위가 `high` 인 근거는 Phase 1 수용 기준 3 이다 — 그 기준이 권한 필터 표면 전부를 시험하되 표면 목록을 필터 정본 조항에 위임하고, 그 조항의 열거가 이 태그 집계를 명시적으로 지목하며, 기준 문면이 *"집계형 표면은 목록 미노출과 수치 미가산을 각각 단언한다"* 로 이 요구의 AC-3·AC-4 를 직접 겨냥한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-18|초판의 "현재 열린 문서의 태그" 규정이 전복되고 전 워크스페이스 전역 집계로 개정됐다|옵시디언 1.8.9 설치본 실측과 독립 검증으로 초판 근거 두 개가 모두 무너졌다. 심의 3인 만장일치이며 이 개정으로 `G11` 이 종결되고 `G31` 이 열렸다 |

### FR-SHELL-010 — 태그 클릭은 좌측 검색 탭을 활성하고 질의를 채운다

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | tag-panel, search, navigation |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

우측 `태그` 탭에서 태그를 클릭하면 좌측 사이드바의 검색 탭이 활성되고 그 태그가 질의로 채워진다. 우측은 색인(어떤 태그가 얼마나 있는가)을 소유하고 좌측 검색 탭은 결과(그 태그가 달린 문서는 무엇인가)를 소유한다.

#### Rationale

2026-08-18 개정으로 규범의 실질은 유지되고 문면 둘이 고쳐졌다. ① 초판의 "흡수"는 "우측에 그 수요를 받을 것이 없다"는 초판 전제의 어휘였고, 이제는 역할 분담이다. ② 초판 근거였던 "탐색 축은 좌측이 소유한다"는 좌측 사이드바 조항 문면에 없는 일반화였다(심의 2인이 독립으로 지적) — 그 조항의 규범은 좌측 3탭의 구조·배치일 뿐이다. 좌측 3탭 구조는 이 동선으로 깨지지 않는다 — 결과 목록은 여전히 좌측 하나이고 우측은 질의어를 건넬 뿐이며, 옵시디언도 좌측에 파일 탐색기·검색·북마크를 두고 우측에 태그를 둔다. 태그 탭이 전역 집계로 개정된 뒤 이 동선은 오히려 더 하중을 받는다 — 이것이 없으면 우측 패널이 자체 결과 목록을 길러 결과 표면이 두 벌이 된다.

#### Acceptance Criteria

- [ ] AC-1: 우측 `태그` 탭에서 태그를 클릭하면 좌측 사이드바가 검색 탭으로 전환된다.
- [ ] AC-2: 전환된 검색 탭의 질의 입력에 클릭한 태그가 채워진다.
- [ ] AC-3: 우측 `태그` 탭은 자체 문서 결과 목록을 갖지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R106-a | informed_by | 원장 조항 R106-a |
| Doc | docs/spec/00.decision-log.md#G31 | related_to | 미결 공백 G31 — 본문 `#태그` 의 렌더·클릭 규정이 없어 태그 클릭의 진입점이 우측 패널 하나로 줄어든다 |
| Requirement | FR-SHELL-009 | depends_on | 태그 목록이 있어야 성립하는 동선이다 |
| Requirement | FR-SHELL-001 | related_to | 결과 목록을 소유하는 좌측 검색 탭이 이 동선의 도착지다 |

#### Research / Analysis

- -

#### Implementation Notes

- 본문의 `#태그` 클릭이 같은 동선을 갖는지는 규정되어 있지 않다(`G31`) — 본문 태그의 렌더·클릭 규정이 어느 조항에도 없고, 이 조항이 전제한 "태그 클릭"의 진입점이 Phase 1 에서 우측 패널 하나로 줄어든다. 본문 태그의 동선을 임의로 만들지 말고 그 공백의 판정을 기다려라. 이 요구의 성숙도가 `evolving` 인 이유가 그것이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-18|초판의 `흡수` 를 `역할 분담` 으로 고치고, 근거의 좌측 소유 일반화를 삭제했다|우측 태그 탭이 전역 집계로 개정되며 초판 전제가 사라졌고, 인용된 좌측 사이드바 조항의 문면에는 그 일반화가 없었다. 규범의 실질(태그 클릭이 좌측 검색으로 간다)은 유지된다 |

### FR-SHELL-011 — 태그 목록의 정렬은 빈도순 기본에 이름순 선택이다 [DRAFT — pending decision]

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | tag-panel, sorting |
| Risk | medium |
| Stability | draft |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

우측 `태그` 탭의 정렬은 빈도순을 기본으로 하고 이름순을 선택으로 제공한다.

#### Rationale

옵시디언 실측에서 태그 패널의 초기 정렬값이 `frequency` 다. 출현 문서 수를 이미 계산하므로 빈도순 정렬은 부수로 따라오고 추가 비용이 0 이다.

#### Acceptance Criteria

- [ ] AC-1: `태그` 탭을 처음 열면 목록이 빈도순으로 정렬되어 있다.
- [ ] AC-2: 정렬을 이름순으로 바꿀 수 있다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R106-d | informed_by | 원장 조항 R106-d — 문면에 〔판단 — 원장 근거 없음〕 마커가 붙어 있다 |
| Requirement | FR-SHELL-009 | refines | 태그 목록의 정렬 축만 정한다 |

#### Research / Analysis

- 옵시디언 1.8.9 설치본 실측에서 태그 패널의 초기 정렬값이 `frequency` 로 확인됐다.

#### Implementation Notes

- **원장 문면에 〔판단 — 원장 근거 없음〕 마커가 붙어 있는 조항이다.** 결정으로 확정된 것이 아니라 실측을 보고 내린 판단이며, 원장이 그 사실을 스스로 명시해 두었다. 그래서 이 요구의 성숙도는 `draft` 이고, 같은 패널의 집계 요구와 달리 착수 전에 결정으로 승격시킬지를 먼저 판정해야 한다. 정렬 축은 집계 축과 독립이므로 이것만 따로 뒤집어도 태그 탭의 다른 규범은 영향을 받지 않는다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### FR-SHELL-012 — 트리 클릭은 활성 탭을 교체하고 Ctrl+클릭은 새 탭이다

| Field | Value |
| --- | --- |
| Type | functional |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | tree, document-tabs |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

좌측 트리에서 문서를 클릭하면 활성 탭을 그 문서로 교체하고, `Ctrl`+클릭은 새 탭으로 연다. 다만 충돌로 자동 저장이 중단된 탭과 저장이 거부된 탭은 자동 교체 대상에서 제외하고 사용자 확인을 요구한다.

#### Rationale

UX 패널 4:0. 브라우저 규약과 동형이라 학습 비용이 0 이고, 딥링크 도입으로 화면이 이미 브라우저처럼 읽힌다. 항상 새 탭으로 열면 트리를 훑는 것만으로 탭이 무한 누적된다. 두 상태에만 확인을 요구하는 이유는 그 둘에서만 되돌릴 수 없는 본문 손실이 실제로 발생하기 때문이다 — 그 밖의 상태는 자동 저장이 손실을 막으므로 확인을 붙이면 이득 없이 마찰만 는다.

#### Acceptance Criteria

- [ ] AC-1: 좌측 트리에서 문서를 클릭하면 새 탭이 생기지 않고 활성 탭의 내용이 그 문서로 바뀐다.
- [ ] AC-2: 좌측 트리에서 `Ctrl`+클릭하면 새 탭이 생기고 그 문서가 열린다.
- [ ] AC-3: 활성 탭이 충돌로 자동 저장 중단 상태이면 트리 클릭이 그 탭을 즉시 교체하지 않고 확인을 요구한다.
- [ ] AC-4: 활성 탭이 저장 거부 상태이면 트리 클릭이 그 탭을 즉시 교체하지 않고 확인을 요구한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R107 | informed_by | 원장 조항 R107 |
| Doc | docs/spec/00.decision-log.md#R107-a | informed_by | 원장 조항 R107-a |
| Requirement | FR-SHELL-005 | depends_on | 교체 대상인 문서 탭이 있어야 성립한다 |
| Requirement | FR-SHELL-001 | depends_on | 클릭 출발점이 좌측 문서 트리다 |

#### Research / Analysis

- -

#### Implementation Notes

- 예외 두 상태의 정의는 이 요구가 소유하지 않는다 — 충돌 감지 시 자동 저장을 일시 중단하는 규칙과, 편집 중 권한 상실·충돌로 저장이 거부됐을 때 본문을 보존하고 로컬 복사·다운로드를 제공하는 규칙이 각각 `EDITOR` scope 에 있다. 그 상태 판정을 여기서 복제하지 말고 조회하라. 두 상태를 놓치면 이 요구가 정확히 본문 손실 경로가 된다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-19 | Created | add-requirement |

### IR-SHELL-003 — 문서 탭 스트립과 문서 헤더는 2단으로 분리한다

| Field | Value |
| --- | --- |
| Type | interface |
| Target | phase-1 |
| Status | planned |
| Priority | medium |
| Tags | document-header, document-tabs, layout |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [셸 화면 설계](./03.screen-design-shell.md) |

#### Requirement

문서 탭 스트립과 문서 헤더를 하나로 합치지 않고 2단으로 분리한다.

#### Rationale

UX 패널 3:1. 탭 스트립은 문서 *간* 이동이고 헤더는 문서 *내* 상태·조작이라 계층이 다르다. 통합하면 두 가지가 깨진다 — ① 탭이 늘 때 브레드크럼과 저장 상태 표시가 먼저 잘린다 ② 헤더 `⋯` 의 공유·버전 조작이 "어느 문서에 대한 것인가"를 흔들어 엉뚱한 문서를 공유하는 경로가 생긴다. 두 번째가 결정적이다 — 되돌릴 수 없는 권한 사고의 경로이기 때문이다.

#### Acceptance Criteria

- [ ] AC-1: 문서 탭 스트립과 문서 헤더가 서로 다른 행에 표시된다.
- [ ] AC-2: 문서 헤더에 그 문서의 브레드크럼과 저장 상태가 표시된다.
- [ ] AC-3: 탭이 늘어나도 문서 헤더의 브레드크럼과 저장 상태가 탭 스트립에 밀려 잘리지 않는다.
- [ ] AC-4: 문서 헤더의 `⋯` 메뉴는 현재 활성 문서를 대상으로 동작한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Doc | docs/spec/00.decision-log.md#R108 | informed_by | 원장 조항 R108 |
| Requirement | FR-SHELL-005 | related_to | 분리 대상 둘 중 하나가 문서 탭 스트립이다 |
| Requirement | FR-SHELL-002 | related_to | 분리 대상 둘 중 다른 하나가 문서 헤더 메뉴다 |

#### Research / Analysis

- -

#### Implementation Notes

- ① 이 요구의 대상은 문서 탭 스트립과 문서 헤더 둘뿐이고 설계서 §1 레이아웃이 그 둘을 중앙 본문 영역에 둔다. 좌·우 사이드바의 단 구성은 이 요구가 정하지 않는다 — 원장 `R108` 문면에 사이드바 한정이 없고, 그 한정은 종결된 `R108-a` 의 옵시디언 실측 서술에만 있어 새 논증의 근거로 인용할 수 없다. ② 좁은 창에서 무엇을 먼저 줄일지는 이 요구가 정하지 않는다. 다만 축약 규칙을 만들 때 브레드크럼과 저장 상태를 먼저 자르면 이 요구의 근거를 정면으로 뒤집는 것이 되므로, 그 순서는 여기서 바꿀 수 없다. ③ Phase 2 분할 창의 pane 최소 폭은 열린 공백(`G30`)이며 Phase 1 범위 밖이다 — 분할이 들어올 때 함께 정해야 하는 축이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-08-18|2단 분리 유지가 실측으로 확인됐다|옵시디언 1.8.9 설치본과 실제 볼트 설정을 원자료로 실측하고 독립 검증자가 재현했다. 조항 문면은 바뀌지 않았고 `G12` 가 종결되며 `G30` 이 열렸다 |
