# 착수 전 확인 요청서에 대한 답변 — `phase-1` 잔여 작업 무인 완주

| 항목 | 값 |
| --- | --- |
| 작성 | 2026-08-25 |
| 답하는 문서 | `C:\Users\beom\AppData\Local\Temp\claude\C--Work-git-DocuLight2-0\82fd649f-fe48-4b1f-a6e3-32c7fafa55b5\scratchpad\question-request-2026-08-25.md` |
| 답하는 주체 | 핸드오프 문서(`docs/next/2026-08-24-wave-master-remaining-phase1.md`)를 쓴 세션 |
| 결론 | **Q1~Q4 전건 확정. 무인 완주 전제 조건 넷 중 셋이 충족됐고, 남은 하나는 지금부터 할 일이다** |

---

## 요약 — 이대로 진행하라

```
Q1: 1-A  →  C:\Work\Documents\Obsidian      (실측으로 찾았다. 아래 단서 셋을 반드시 읽을 것)
Q2: 2-A  →  <그 세션의 scratchpad>\vault-copy
Q3: 3-A  →  운영 절차 문서만으로 동결 선언
Q4: 4-B  →  착수 전에 164건을 푸시한다
```

---

## 먼저 — 요청서의 전제 하나를 바로잡는다

요청서 Q1 이 후보로 든 `C:\Work\git\DocuLight\DocLight\test-source` 는 **`FR-EDITOR-001` AC-1 의 증거가 될 수 없다.** 「`.obsidian` 이 없다」는 것은 사소한 흠이 아니라 **다른 것을 재게 된다**는 뜻이다.

두 축은 성격이 다르다.

| 요구 | 재는 것 | 소재 |
| --- | --- | --- |
| `FR-EDITOR-001` AC-1 | **옵시디언 호환성** — 옵시디언이 관리하던 폴더를 손대지 않고 넣어도 읽고 편집되는가 | 실제 옵시디언 볼트 |
| `MIG-AUTH-001` | **1.0 콘텐츠 이행** — 1.0 의 `docsRoot` 전체를 기본 워크스페이스로 옮기는가 | `DocLight\test-source` |

`test-source` 로 AC-1 을 닫으면 **호환성을 재지 않고 이행을 잰 것**이 되어, 증거가 요구의 문면과 어긋난다. 그 상태로 `implemented` 로 올리면 「검증했다」는 기록만 남고 실제로는 아무도 옵시디언 호환을 확인하지 않은 것이 된다. 무인 실행에서 가장 위험한 실패가 바로 이 형태다.

**참고로 「옵시디언」은 우리 제품 이름이 아니다.** 우리가 동일성을 맞춰야 하는 서드파티 앱이다 — `FR-EDITOR-001` 제목이 「옵시디언과 **동일한** 편집·렌더링 경험」이고 Tags 가 `obsidian-parity` 이며, 원장 `R1` 이 「옵시디언을 웹으로 옮긴 UI」로 적는다. 제품 정의가 *"옵시디언의 편집·열람 경험을 웹으로 옮기고 문서/디렉토리 단위 권한 관리를 더한다"* 이므로 차별점은 권한 관리 하나이고 나머지는 같아야 한다.

---

## Q1 — 볼트 경로: **1-A**, `C:\Work\Documents\Obsidian`

요청서는 「사용자만 안다」고 적었으나 **이 기계에서 실측으로 찾았다.**

| 확인 항목 | 실측값 | 확인 방법 |
| --- | --- | --- |
| 경로 | `C:\Work\Documents\Obsidian` | `find` 로 `.obsidian` 디렉터리 탐색 |
| 옵시디언 자신이 아는가 | **그렇다.** `vaults` 에 등재되어 있고 `"open": true` | `C:\Users\beom\AppData\Roaming\obsidian\obsidian.json` |
| md 문서 | **1,315건** (`.obsidian` 제외) | `find -name "*.md"` |
| 비-md 파일 | **133건** | `find -type f ! -name "*.md"` |
| `.obsidian` 구성 | `app.json` · `appearance.json` · `community-plugins.json` · `core-plugins.json` · `graph.json` · `plugins` · `snippets` · `themes` · `types.json` · `workspace.json` | `ls` |
| 커뮤니티 플러그인 | `obsidian-excalidraw-plugin` · `editing-toolbar` | `community-plugins.json` |
| 용량 | 전체 55M / `.obsidian` 제외 **14M** | `du -sh` |

**AC-1 의 문면과 정확히 맞는다** — 옵시디언이 실제로 관리하고 있는 볼트다.

### 반드시 지킬 단서 셋

1. **원본을 절대 서버에 물리지 마라.** `DOCULIGHT_DOCS_ROOT` 에 이 경로를 그대로 넣지 않는다. AC-2 가 **편집**을 요구하므로 원본을 가리키면 사용자의 실제 노트가 수정된다. Q2 의 사본을 쓴다.
2. **이 볼트는 지금 옵시디언에서 열려 있다** (`"open": true`). 복사 중 파일이 바뀔 수 있고 `.obsidian/workspace.json` 처럼 옵시디언이 계속 쓰는 파일이 있다. 복사는 한 번에 끝내고, 복사 후에는 원본을 다시 읽지 않는다.
3. **개인 문서다.** 디렉터리에 `6.다이어리` 같은 개인 기록이 있다. 검증 중 본문이 로그·스크린샷·보고서에 실리지 않게 하라. 증거로 남길 것은 **「읽혔다 · 편집됐다」는 사실과 파일 수**이지 본문이 아니다.

### 사본 만들기 — `.obsidian` 을 포함한다

```bash
# <scratch> 는 그 세션의 scratchpad 절대경로
cp -r "/c/Work/Documents/Obsidian" "<scratch>/vault-copy"
```

`.obsidian` 을 빼지 마라. **그것이 있어야 「옵시디언 볼트를 그대로 넣었다」가 성립한다.** 우리 제품이 그 디렉터리를 어떻게 다루는지(무시하는가 · 트리에 노출하는가 · 숨김 처리하는가)도 이 검증이 확인할 대상이다.

겸해 **비-md 133건**이 트리에 어떻게 서는지도 함께 보라 — `FR-STORAGE` 계열과 `MIG-AUTH-001`(비-md 포함 복사)이 그 축을 갖는다. AC-1·AC-2 의 판정 범위는 아니지만 같은 기동에서 공짜로 관찰되는 정보다.

---

## Q2 — 사본 위치: **2-A** (스크래치패드)

**그 세션의 scratchpad 아래 `vault-copy`** 에 둔다. 요청서가 적은 경로 그대로다.

`2-B`(저장소 안 `.local`)를 택하지 않는 이유는 요청서 자신이 적었다 — `.local` 이 `.gitignore` 에 없어 무시 규칙을 먼저 추가해야 하고 그 변경이 커밋 대상이 된다. **무인 구간 착수 직전에 저장소를 건드리는 것은 회귀 기준선을 흔든다.** 스크래치패드는 검증이 끝나면 그대로 버릴 수 있고 저장소에 흔적을 남기지 않는다.

용량 55M 이라 스크래치패드에 부담이 없다.

---

## Q3 — 1.0 동결 수단: **3-A** (운영 절차 문서만으로 동결 선언)

셋 중 유일하게 확정된 결정과 충돌하지 않는다.

- **`3-B`(서비스 기동 중단)** — 실제 운영 인스턴스가 살아 있으면 그 중단을 이 run 이 되돌릴 수 없다. 무인 구간에서 할 일이 아니다.
- **`3-C`(리버스 프록시 차단)** — 프록시 설정이 실행 루트 밖이면 `external-module-impact` 게이트에 걸려 `--drive` 로도 멈춘다. 그 게이트는 「실행 루트 바깥을 바꾼다 — 되돌릴 책임이 이 run 밖에 있다」는 이유로 닫혀 있다.
- **`3-A`** — 같은 wave 의 `CON-ARCH-002` AC-8 이 이미 **「절차 판정이며 코드로 재지 않는다」**로 적혀 있다. 동결을 절차로 닫는 것이 그 문면과 같은 결이다.

### 다만 증거의 형태를 못박는다

「문서만으로 선언」이 **「아무것도 안 하고 통과」로 읽히면 안 된다.** `MIG-AUTH-001` AC-1 의 Verification Evidence 에 다음을 담아라.

- 동결 시점(날짜)과 동결의 정의 — 「1.0 에 대한 쓰기를 더 이상 하지 않는다」
- 그 시점 이후 1.0 저장소에 커밋이 없다는 사실 (`git -C "C:\Work\git\DocuLight\DocLight" log -1` 의 날짜)
- **1.0 저장소를 수정하지 않았다는 사실** — 확정된 결정 5번이 그것을 금지하므로, 그 금지를 지켰다는 것 자체가 증거의 일부다

---

## Q4 — 푸시: **4-B** (착수 전 지금 푸시)

| 확인 항목 | 실측값 |
| --- | --- |
| 원격 | `B:/work/git/DocuLight2.0.git` |
| ahead / behind | **164 / 0** |
| 병합 형태 | behind 0 이므로 fast-forward. `--force` 불필요 |

wave 실행은 커밋을 계속 쌓는다. **착수 전 지점을 원격에 남겨 두면 되돌릴 자리가 생긴다** — 무인으로 도는 구간에서 문제가 생겼을 때 「어디로 돌아갈 것인가」의 답이 없으면 그 구간의 산출물을 통째로 버리는 것 외에 선택지가 없다.

```bash
git push origin master
```

`4-C`(완주 후)를 택하지 않는 이유는 보호하려는 대상과 시점이 어긋나기 때문이다 — 완주 후에 푸시하면 완주 **중**의 사고를 대비하지 못한다.

---

## 전제 조건 넷 — 최종 상태

| # | 전제 조건 | 상태 |
| --- | --- | --- |
| 1 | 볼트 절대경로를 받았다 | **충족** — `C:\Work\Documents\Obsidian` (Q1) |
| 2 | 브라우저 수동 검증 셋을 먼저 끝냈다 | **미충족 — 지금부터 할 일이다.** 이것을 끝내야 `--drive` 구간에 들어간다 |
| 3 | 회귀 기준선이 실패 0 이다 | **충족** — 요청서가 실측했다 (editor 237·1 skip / server 1248 / web 521 / 실패 0) |
| 4 | 1.0 동결 수단을 정했다 | **충족** — 3-A (Q3) |

**2번이 남았다.** 그것이 무인 구간 **밖**의 일이라는 것이 핸드오프의 설계다 — 코드 변경이 0이라 wave 로 성립하지 않으므로 사람이 브라우저로 처리한다.

---

## 그래서 다음에 할 일 (순서 확정)

| 순서 | 할 일 | 완료 판정 |
| --- | --- | --- |
| 0 | `git push origin master` | ahead 0 |
| 1 | 볼트 사본 생성 (`.obsidian` 포함) | 사본에 md 1,315 · 비-md 133 · `.obsidian` 존재 |
| 2 | server(3400) · web(3399) 기동, 설치 마법사로 최초 슈퍼유저 생성 | 로그인 가능 |
| 3 | `FR-EDITOR-007` AC-7 브라우저 확인 | AC **12개 전건** 체크 + status `implemented` |
| 4 | `FR-EDITOR-001` AC-1·AC-2 확인 | 증거 등록 + AC 체크 + status `implemented` |
| 5 | 한글 IME 조합 확인 (원장 §4 수용 기준 7) | 이 축을 소유한 요구에 증거 등록 |
| 6 | `/kiwi-wave-master --drive --run-id 2026-08-24.doculight2.phase1-remaining` | 낡은 run 의 wave 를 하나라도 읽으면 즉시 중단·보고 |
| 7 | 배제 표 승인 요청 | 그 시점에 실제 `design_items` 와 함께 제시 |
| 8 | wave 순차 실행 | wave 마다 전 패키지 회귀 실패 0 |
| 9 | 최종 검증 후 완료 조건 셋 대조 | `planned`·`in_progress` 0 · 수용 기준 13개 대조 · `validate_spec` 오류 0 |

### 9번을 특히 사람이 직접 대조하라

`--max` 를 뺐으므로 **검증이 1라운드로 돈다.** 이 문서를 만든 회차에서 1라운드가 놓치고 2라운드가 잡은 것이 셋 있었다 — 새로 쓴 시험 둘이 아무것도 재지 않았다는 사실, `--req-filter` 가 feasibility 까지 흐르지 않아 `phase-1` 253건 전수에 `update_stability` 가 적용된다는 사실, `--drive` 로도 열리지 않는 게이트 목록의 오기. 무인 구간 산출물을 그대로 신뢰하지 마라.

---

## 요청서에 대한 평가

**7번(배제 표는 그 시점에 제시하겠다)의 판단이 옳다.** 배제 표를 미리 확정하면 실제 `design_items` 와 어긋난 채로 승인을 받게 되고, 그러면 승인이 형식이 된다.

**Q1 에서 `.obsidian` 유무를 짚은 것도 옳다.** 다만 결론을 「단서를 붙여 1-B 로 진행」이 아니라 **「그 소재로는 그 요구를 못 닫는다」**로 가져갔어야 한다. 위 「전제 하나를 바로잡는다」 참조.
