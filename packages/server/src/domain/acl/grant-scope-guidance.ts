/**
 * 「하위는 빼고 이 폴더에만 주고 싶다」는 수요에 대한 **정본 안내**
 * (`CON-ACL-003` AC-3 · AC-4).
 *
 * 그 선택지를 두지 않는 이유는 그것이 하위에 대한 **거부**이기 때문이다 —
 * 거부 규칙 부재 원칙(`CON-ACL-002`)이 정면으로 금지한다.
 *
 * 문구를 화면이 아니라 여기 두는 이유는 두 가지다. 하나는 이 문구가 지켜야
 * 할 제약(금지어·긍정형·세 경로)이 요구사항이라 화면마다 다시 지킬 수
 * 없다는 것, 다른 하나는 이 수요가 여러 화면에서 같은 말로 답해져야 한다는
 * 것이다.
 *
 * 실제 화면 문안은 이 값을 **그대로** 쓴다 — 화면이 고쳐 쓰기 시작하면
 * 금지어 검사가 통과한 문구와 사용자가 읽는 문구가 갈린다.
 */
export const GRANT_SCOPE_GUIDANCE = {
  /**
   * 긍정형으로만 쓴다 (AC-3) — 무엇을 막는지가 아니라 무엇을 여는지로
   * 말한다. 부여는 언제나 넓히기이므로 그것이 사실에도 맞는다.
   */
  body: '권한 부여는 지정한 노드와 그 하위 전체를 함께 엽니다. 더 좁은 범위가 필요하면 아래 세 가지 중 하나를 쓰십시오.',

  /** 세 경로 (AC-4). */
  paths: [
    '하위의 특정 노드에서 상속을 끊고 그 노드의 권한을 따로 지정합니다 — 워크스페이스 관리자가 실행합니다.',
    '디렉토리 대신 열려는 개별 문서에 직접 부여합니다. 그러면 경로상의 디렉토리는 이름만 보입니다.',
    '경계가 상시 필요하면 그 영역을 별도 워크스페이스로 분리합니다.',
  ],
} as const;
