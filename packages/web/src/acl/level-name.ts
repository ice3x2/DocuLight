/**
 * ACL 레벨의 화면 이름 (`IR-AUDIT-004` AC-7).
 *
 * **여기 하나다** — 두 곳에 적으면 두 화면이 같은 레벨을 다른 말로 부르고,
 * 레벨이 하나 늘 때 한쪽만 늘어 그 어긋남을 아무도 눈치채지 못한다.
 *
 * 모르는 값은 **그대로 돌려준다.** 감사 낱행의 레벨은 개정 전 계약으로
 * 기록된 값을 담을 수 있고(`DR-AUDIT-002` AC-8 뒷문장 — 이미 기록된 행은
 * 고치지 않는다), 그때 지어낸 이름으로 덮으면 감사가 사실과 어긋난다.
 *
 * `NewWorkspaceForm` 의 `레벨문구` 와 다른 것이다 — 그쪽은 `default` 그룹의
 * 초기 권한이라 `없음` 을 담고 `관리` 를 담지 않는다.
 */
const 이름: Record<string, string> = {
  view: '보기',
  edit: '편집',
  admin: '관리',
};

export const ACL레벨이름 = (level: string): string => 이름[level] ?? level;
