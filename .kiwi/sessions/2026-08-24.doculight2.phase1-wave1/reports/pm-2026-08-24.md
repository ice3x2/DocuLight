# kiwi-pm 종료 보고 — 2026-08-24.doculight2.phase1-wave1

## §1 요약

| 항목 | 값 |
|---|---|
| Task | 16 (done 16 · skipped 0 · failed 0 · blocked 0) |
| Phase | 3 (PH-001 서버 배선 · PH-002 화면 · PH-003 조립 방벽) |
| tdd_policy | strict — 면제 0건 |
| 검증 | typecheck 0 · server 1233 · web 516 · editor 237(+1 skip) |
| 뮤테이션 탐침 | 22건 시도, 전부 DEAD (그중 2건은 처음 살아남아 판정을 고쳤다) |

## §2 Task 별 결과

PH-001 여덟은 red→green 페어로 돌았다. T-PH001-07/08 과 T-PH003-01/02 는 앞선 Task 의 구현이 이미 그 조건을 만족시켜 **red 가 서지 않았다** — 그 사실을 숨기지 않고 뮤테이션으로 시험의 유효성을 따로 확인했다. T-PH003-01 은 그 확인에서 처음 판정이 틀렸음이 드러나(참조 존재 → 도달 가능성) 다시 썼다.

## §3 req_coverage

| REQ | 진입 | 종료 | 비고 |
|---|---|---|---|
| SEC-AUTH-010 · 011 · 012 · 013 · 014 · 015 · 017 | implemented | implemented | 전건 새 증거 등록. 앞선 증거는 서비스 계층 단위 시험뿐이었고 제품 조립을 재지 않았다 |
| FR-CONFIRM-019 | in_progress | **implemented** | AC-6 종결 |
| OBS-AUDIT-001 | in_progress | **implemented** | 앞선 커밋에서 R165 판정으로 종결 |

## §4 SRS mutation

`add_verification_evidence` 6건 · `update_status` 1건(FR-CONFIRM-019) · `add_completed_work` 1건. 보류 0건.

일곱 SEC-AUTH 요구는 이미 `implemented` 였으므로 승급하지 않았다 — **그 상태가 사실과 어긋나 있었고**, 이 wave 가 그것을 사실로 만들었다. 상태를 되돌렸다가 다시 올리는 것은 장부에 없던 사건을 만드는 일이라 하지 않고, 대신 새 증거로 그 사실을 고정했다.

## §5 게이트

lifecycle gate 통과(전건 stable/evolving, 차단 0). validator `exit_code != 0` 은 WARN 으로 진행했다 — C15 의 evidence 축이며 계획 단계에 의도한 편차로 기록돼 있다.

## §6 독립 검증이 잡은 것

| 등급 | 내용 | 처분 |
|---|---|---|
| CRITICAL | 허용목록이 `/install-assets`, 빌드는 `/assets/*` — 설치 화면이 껍데기만 받았다 | 고침 + 시험을 빌드 산출물 기준으로 |
| CRITICAL | 503(설치 전)이 설치 화면으로 가지 않아 영원한 로딩 | 고침 + 시험 추가 |
| HIGH | 경계에서 열거를 세우지 않아 `default` 그룹이 `admin` 을 받았다 | 고침 + red 시험 |
| HIGH | 방벽이 `http/guards/` 를 안 봐 미배선 보안 가드 둘을 숨겼다 | 시야를 `http/` 로 넓힘 |
| HIGH ×3 | 공허한 단언 셋 | 관측 가능한 대상으로 교체 |
| MEDIUM ×5 | 지연 고지의 거짓 · 실패 침묵 · rate limit 부재 · AC 미측정 둘 | 전부 처분 |

## §7 잔여

방벽 허용목록 여섯 — 가입 · 비밀번호 · PAT · 감사 보존 일소 · 문서 원문 서빙 라우터 · 그 서빙 가드 둘. 전부 사유와 함께 코드에 고정돼 있고 목록이 줄어드는 것이 다음 wave 의 정의다. 휴지통 보존 일소도 같은 상태이나 모듈 단위 판정이 못 잡아 주석에 적었다.

## §8 checklist

plan.md 는 체크박스 형식이 아니라 `pm-state.json` 이 진행의 SSOT 다.
