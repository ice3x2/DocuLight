# Issue #68 Astra decision — final independent review\n\nReviewer: Sol\nDate: 2026-09-18\nScope: final preimplementation decision review only\n\n## Verdict\n\n**PASS — Critical 0 / High 0 / Medium 0 / Low 0.**\n\nThe previous Medium finding is resolved. The separated integration work now has two explicit, non-overlapping owners: live #81 with planned/stable `IR-PRINCIPAL-003`, and live #82 with planned/stable `IR-PRINCIPAL-004`. Their acceptance criteria and trace links cover every functional defect withdrawn from #68 without claiming either follow-up is implemented or verified. Issue #68 can therefore close independently after its bounded presentation, tests, browser evidence and review pass; #81/#82 implementation or closure is not a prerequisite.\n\n## #68 bounded closure\n\nThe live #68 issue names `GroupRoster.tsx` as an independently completable design unit. The final Astra decision stays within that boundary: `GroupRoster.tsx`, its scoped stylesheet, focused tests and browser-harness assets may change, while App/AppShell/query/client/server/shared-picker/shared-confirmation product implementations remain outside #68.\n\nThe current props support a truthful presentation subset. Supplied groups and members can be rendered, the existing `PrincipalPicker` can dispatch an existing user candidate with the target group ID, and the component can expose the current shared search states. They cannot represent group-query phase, mutation result, exact delete impact, system membership capability or post-response focus. The decision neither fabricates those states nor treats callback dispatch as a successful server mutation.\n\nThe ordinary-group delete decision is safe and requirement-aligned. The current one-click `onRemove` path violates `FR-CONFIRM-006` AC-5 because group deletion is L3. A visible, genuinely disabled ordinary-group button with no handler and an independently readable explanation removes that unsafe execution path while preserving the public callback signature for #82. System-group delete remains absent under `CON-PRINCIPAL-002`. This does not claim ordinary groups are immutable or weaken the existing atomic delete service in `FR-PRINCIPAL-002`.\n\nThe local `row.kind === 'user'` guard is also within #68. It enforces the existing user-ID callback boundary and `DR-PRINCIPAL-002` without adding a local fetch, filtering the shared result set, changing `PrincipalPicker`, or claiming a user-only picker. A group candidate remains visible as an unsupported shared-picker result but cannot reach `onAddMember`; the actual user-only mode belongs to #81.\n\n## Follow-up ownership and feasibility\n\n`IR-PRINCIPAL-003` / #81 owns group query loading/error/ready, real retry, Promise-backed member addition, same-group duplicate guards, write-versus-refresh separation, stale generation and focus handling, shared user-only picker configuration, existing-member exclusion, authoritative system subtype/capability/effective-member projection, role loss and actual App-path evidence. Its ten ACs trace to `FR-PRINCIPAL-001`, `DR-PRINCIPAL-002`, `CON-PRINCIPAL-002`, `CON-PRINCIPAL-006`, `IR-SHELL-002` and `IR-SHELL-006`.\n\nThe scope is feasible with the current architecture: React Query already owns the roster query, App currently owns add-member invalidation, `PrincipalPicker` already owns threshold/cap/order/privacy/stale-search behavior, and the server already enforces user-only membership and superuser authorization. The new projection and narrow picker configuration are explicitly required instead of inferred from `system`, group names or client constants. The requirement does not claim those additions exist today.\n\n`IR-PRINCIPAL-004` / #82 owns the authoritative delete preview, fresh group identity/name/system/member/complete-ACL-count contract, exact-name L3 consent, stale consent lock, one captured-ID delete, existing atomic group-plus-ACL transaction, write-versus-refresh separation and post-delete focus. Its ten ACs trace to `FR-PRINCIPAL-002`, `CON-PRINCIPAL-002`, `FR-CONFIRM-006`, `FR-CONFIRM-004`, `FR-CONFIRM-001` and `IR-SHELL-006`.\n\nThat scope is feasible and does not overclaim the current revocation preview. The existing group DELETE and database cascade remain the execution path, while #82 must add a complete impact projection because revocation rows can omit unplaceable ACL entries. Zero ACL rows do not lower the L3 grade, member accounts remain, and bulk revoke is not used as a preparatory write.\n\nThe final decision maps every separated item to exactly one of these requirements. Query/member-add/user-only/B1 and their lifecycle belong to #81; delete impact/L3/transaction/deletion focus/B2 belong to #82. Group creation/rename, member removal/undo, invitations and batch operations are explicit non-goals rather than falsely assigned work. Their future implementation still requires its own covering SRS and issue.\n\n## Presentation, accessibility and evidence plan\n\nThe exact Korean copy is internally consistent and does not claim effective-member completeness, successful persistence or completed deletion. Supplied members remain a semantic readable list in supplied order; empty explicit membership is not described as absence of effective membership. System status derives only from the supplied `system` flag, and no name/ID inference is allowed.\n\nThe ten-state matrix covers default, hover, focus, selected, disabled, readonly, invalid, loading, empty and error with explicit applicability or a separate owner. It keeps disabled explanations outside hover-only affordances, preserves keyboard access and visible focus for supported controls, avoids a fake focus target for disabled delete, distinguishes no supplied data from query-success empty, and leaves unsupported mutation/query outcomes to #81/#82.\n\nThe browser plan satisfies the live issue and governing shell requirements. Every one of the 12 viewport/theme/zoom environments uses the production `AppShell`, opens settings through the actual trigger, and checks superuser, ordinary non-superuser and non-superuser workspace-manager fixtures. This demonstrates `IR-SHELL-002` AC-6 across the same actual-product surface used for the visual evidence; it does not claim backend authentication evidence.\n\nThe harness constraints are adequate: fresh Playwright-owned isolated Chromium, no existing profile or CDP attachment, no OS/native input, disposable persistent context and extension for real 200%, `setZoom` followed by a separate read-only `getZoom`, reset followed by another separate `getZoom`, same-mounted-tree resize and 100→200→100 transitions, forced-colors coverage, and same-process or otherwise identified owned cleanup. Synthetic composition/DOM events are correctly labelled; native Windows IME, candidate windows, password-manager and autofill UI remain non-blocking untested limitations and are not claimed as automated evidence.\n\n## Spec validation\n\nThe index reflects the two additions: interface requirements increase from 29 to 31 and planned requirements from 17 to 19. The phase-1 summary reports `IR-PRINCIPAL-003` and `IR-PRINCIPAL-004` as stable planned new-work candidates, with no draft/deprecated blocker and no missing evidence claim for planned work.\n\n`speckiwi validate --fail-on-warning --json` reports zero errors and the pre-existing `SRS-W072` warning for `02.feature-request-live-preview.md` sharing leading number 2. `speckiwi links check --json` checks 1,095 references and reports the pre-existing unrelated `IR-AUDIT-004` reference `32` as an invalid GitHub issue URL; neither diagnostic was introduced by #81/#82 or the #68 decision. The new requirement issue URLs are canonical and both live issues were read directly.\n\nNo product code, SRS, GitHub issue, commit or push was changed by this review. Only this review document was replaced.\n\n## Sources reviewed\n\n- `AGENTS.md`\n- `docs/spec/00.index.md`\n- live GitHub issues #68, #81 and #82\n- `.kiwi/sessions/newspaper-20260916/evidence/issue68/astra-decision.md`\n- `docs/spec/16.principal.srs.md` — `FR-PRINCIPAL-001`, `DR-PRINCIPAL-002`, `CON-PRINCIPAL-002`, `FR-PRINCIPAL-002`, `CON-PRINCIPAL-006`, `IR-PRINCIPAL-003`, `IR-PRINCIPAL-004`\n- `docs/spec/11.confirmation-grades.srs.md` — `FR-CONFIRM-001`, `FR-CONFIRM-004`, `FR-CONFIRM-006`\n- `docs/spec/08.app-shell.srs.md` — `IR-SHELL-002`, `IR-SHELL-006`\n- current `GroupRoster`, `PrincipalPicker`, App, AppShell, client/query, server roster/membership/delete implementations and relevant tests\n\n