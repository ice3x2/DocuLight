# GitHub #66 / #79 relocation integration contract — final independent review\n\nReviewer: Sol\nDate: 2026-09-18\nScope: live GitHub #66 and #79, final `astra-decision.md`, `IR-SHELL-011`, SRS index/trace, current relocation API/UI contracts\nChange boundary: review document only; no product, SRS or GitHub mutation\n\n## Verdict\n\n**PASS — Critical 0 / High 0 / Medium 0 / Low 0.**\n\nLive #79 and `IR-SHELL-011` now fully own the actual App integration separated from #66 without reopening #66's presentation scope. The previous High 1, High 2 and Medium 1 findings are closed. No new contradiction, unsupported API/policy claim or missing implementation boundary was found.\n\n## Closure of prior findings\n\n### Previous High 1 — implementation file authority\n\nClosed. Live #79 now explicitly includes:\n\n- `packages/web/src/App.tsx`\n- `packages/web/src/shell/AppShell.tsx`\n- `packages/web/src/api/client.ts`\n- `packages/web/src/shell/RelocationDialog.tsx`\n- `packages/web/src/acl/RelocationPreview.tsx`\n- narrowly scoped relocation state and tests\n\nThe issue and `IR-SHELL-011` Implementation Notes restrict the two presentation components to the minimum integration props for authoritative query/mutation state, actual result transport, execution lock and parent-focus callback. They explicitly preserve #66's visual result and prohibit destination-projection redesign. This is sufficient authority to implement AC-2, AC-4, AC-5, AC-6, AC-11, AC-12 and AC-13 without expanding into unrelated UI or server work.\n\n### Previous High 2 — handed-off integration obligations\n\nClosed.\n\n- AC-11 and #79 distinguish destination-query loading/error/retry/ready-empty from preview state and post-write tree refetch. Empty is authoritative only after ready-empty, and retry repeats GET without a write.\n- AC-12 and #79 require the real caller source-node level and show manager simulation guidance only for `level=admin`; counts, defaults and other state cannot infer admin.\n- AC-13 and #79 require parent focus restoration after form cancel, L2 cancel, mutation failure and accepted success, with source-row restoration when available and a predetermined logical tree fallback otherwise.\n\nThese contracts use existing boundaries rather than inventing capability: AppShell already holds the source `TreeNodeView`, `treeState` already represents authoritative tree query state, and DocumentTree already has a focus-request/fallback pattern that can be extended narrowly.\n\n### Previous Medium 1 — trace and maturity\n\nClosed. `IR-SHELL-011` now traces all governing contracts requested by the final #66 decision:\n\n- `FR-SHELL-015`\n- `FR-ACL-002`\n- `FR-ACL-006`\n- `FR-CONFIRM-005`\n- `FR-CONFIRM-016`\n- `FR-CONFIRM-017`\n- `SEC-ACL-014`\n- `SEC-ACL-015`\n- `SEC-SHELL-002`\n- `SEC-SHELL-003`\n- `IR-SHELL-008`\n\nThe link relations and notes match their source requirements: copy preview remains destination-derived and visible to a source-view-only caller with destination edit; the form remains separate from the L2 gate; move/copy permission and privacy boundaries stay server-owned; overlay/focus behavior depends on the shared shell contract.\n\n`Status=planned` is correct because no implementation or evidence is claimed. `Stability=stable` is now reasonable: the behavior, exclusions, API boundary, state ownership, confirmation transition, privacy rules, focus behavior and evidence method are decided, while implementation remains pending.\n\n## Contract consistency\n\n- #66 remains independently completable as the existing-prop presentation issue. #79 owns live transport and lifecycle and does not retroactively make integration a #66 closure prerequisite.\n- Existing server contracts are described accurately. Relocation preview already returns server-owned kind/count/grade; move returns `{name}`; copy returns `{id,name,copied}`. Neither #79 nor `IR-SHELL-011` creates a new endpoint or authorization policy.\n- Fresh move L1 requires a deliberate current intent and executes with no open `ConfirmGate`. L2 and every copy use a literal L2 gate. An open L2 that receives L1 never forwards L1 into the shared gate or autoexecutes; a new explicit action is required.\n- Mutation pending, duplicate prevention, uncertain network outcome, stale generation, failure retry and successful-write/refetch separation are specified without automatic write retry or false cancellation claims.\n- Every successful result uses the server-returned resolved name. The common result-name clause does not compare against stale source text, infer collision cause, expose a hidden target or add a confirmation.\n- Same-workspace move, cross-workspace copy, source-view/destination-edit, visible-only copy, automatic suffix, count-versus-roster privacy and hidden-object non-enumeration remain authoritative existing contracts.\n- Destination search, hierarchical picker, root edit projection, undo, collision reason, automatic copy opening and audit deep link remain explicitly out of scope.\n\n## Browser and process evidence boundary\n\nLive #79 and AC-10 correctly require the actual App path in fresh Playwright-owned isolated Chromium. Real 200% uses a disposable persistent profile extension calling `chrome.tabs.setZoom(2)` and independently asserting `getZoom()===2`. Existing/default browsers, CDP attachment and OS/native input automation are prohibited by the issue.\n\nNative Windows IME candidate UI remains a non-blocking unverified limitation. Only explicitly labelled synthetic composition/DOM evidence is authorized. Cleanup is limited to ownership-proven test PIDs; broad termination of `node.exe` or Chromium is prohibited.\n\n## Index, validation and links\n\n- `docs/spec/00.index.md` correctly changes planned requirements from 15 to 16 and interface requirements from 27 to 28.\n- Active target `phase-1` reports `IR-SHELL-011` as a stable planned new-work candidate, with no stability blocker or warning for this requirement.\n- `speckiwi validate --json`: 0 errors and one pre-existing unrelated `SRS-W072` warning for `02.feature-request-live-preview.md` sharing leading number 2.\n- `speckiwi links check --json`: 1069 references checked. The only broken reference is the pre-existing unrelated `IR-AUDIT-004` GitHub issue value `32` (`SRS-W004`). The `IR-SHELL-011` GitHub URL resolves to live open #79, and its related #66 decision document exists.\n- Work mode is `sdd`; active target is `phase-1`.\n\n## Final disposition\n\n`IR-SHELL-011` and live #79 are sufficiently complete, bounded and reviewable to proceed to strict test-first implementation. Verification evidence remains empty and every AC remains unchecked, so this PASS approves the requirement/issue design only. It does not claim implementation, test execution, requirement completion or issue closure.\n\nNo product code, SRS document, GitHub issue, commit or push was changed by this review.\n