Command: node run-product-fixture.mjs
Working directory: .kiwi/sessions/newspaper-20260916/evidence/issue58
Exit code: 0
{"pass":true,"exactBinaryBytes":true,"targetIdentityRetained":true,"binarySurfaceRefreshed":true,"markdownSurfaceRefreshed":true,"markdownVersionRetained":true,"totalNewVersionRequestCount":2,"viewOnlyActionDisabled":true,"serverDenied403":true,"productMatrixCount":12}

The two new-version requests are exactly one accepted binary replacement plus one direct Markdown replacement. Binary selection before L2 acceptance emitted zero requests. The product UI then reopened the retained binary target as its download surface and displayed the replaced Markdown body in CodeMirror, observing both query refreshes rather than relying only on direct API reads. The same disposable product ran a 12-environment full-AppShell matrix; every case used a unique persistent profile and asserted the three shell panels, image surface, picker disabled/hover state, warning/dialog contrast and L2 overlay. Each product alert scrolls the cancel action into the visible alert/viewport bounds and activates it; `product-actions-*.png` records the reached action row. The product matrix supplies decoded image response bytes through Playwright routing because the frozen #58 scope does not add a node-content API; exact stored and downloaded bytes are proven separately. Both authenticated accounts used separate Playwright persistent contexts with unique temporary profiles. The disposable server, Vite process, profiles and data directory were owned by this fixture and removed in finally blocks.
