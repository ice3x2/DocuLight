# Issue 57 TDD chronology

| Final behavior/assertion | Pre-implementation RED | GREEN evidence |
| --- | --- | --- |
| Rejected save uses the approved generic Korean message and exposes `편집 중인 본문 내려받기` | `red-contract.txt` — DocumentSurface rejected-state assertion | `green-targeted-final.txt`; `green-playwright-layout-final.txt` |
| Download reads the latest editor source after further editing and preserves exact bytes | `red-contract.txt` — rejected rescue test failed first at the missing approved contract; the same test already contained the exact-byte assertion before implementation | `green-targeted-final.txt`; fixture and actual ACL path in `authenticated-product-final.txt` |
| Conflict strip uses the complete warning, server/local labels, and approved resolve instruction | `red-contract.txt` — conflict contract assertions; `red-playwright-layout.txt` — actual component failed at exact warning | `green-targeted-final.txt`; `green-playwright-layout-final.txt` |
| Merge acceptance passes exact current right-pane bytes | The conflict test in `red-contract.txt` contained the exact-byte assertion before implementation but stopped at earlier missing conflict copy | `green-targeted-final.txt`; real persistence in `authenticated-product-final.txt` |
| Replacement confirmation has the approved accessible title/description, initial cancel focus, destructive accept, and Escape cancel | `red-contract.txt` — missing dialog heading/initial focus and Escape cancellation failures | `green-targeted-final.txt`; `green-playwright-layout-final.txt` |
| Composition Enter does not accept; a later separate Enter accepts once | After the first review identified that the original RED stopped early, the composition and one-shot implementation was removed. `red-focused-confirmation.txt` reaches the composition assertion and shows Enter called accept | `green-focused-confirmation-url.txt`; `green-playwright-visual-expanded.txt` |
| Rapid activation uses the current callback exactly once | With the one-shot guard removed, `red-focused-confirmation.txt` reaches the callback-count assertion and reports two calls while also confirming the stale callback was not used | `green-focused-confirmation-url.txt`; `green-playwright-visual-expanded.txt` |
| Conflict/rejected/merge-action computed geometry and semantic styles | The entire new issue 57 CSS slice was removed. `red-focused-computed-style.txt` reaches the computed warning padding assertion and reports `0px !== 12px` | `green-playwright-visual-expanded.txt`, including semantic colors, contrast, and focus-ring bounds |
| Canceled composition does not schedule an unchanged-body save | `red-composition-save-count.txt` reaches live and source cancel assertions with exact text/caret preserved but `save PUT delta=1` | `green-composition-save-count.txt` records cancel PUT delta 0, committed Enter PUT delta 1, and merge/dialog PUT delta 0 |
| Cancel restores a usable invoking focus target | `red-product-focus-restore.txt` — actual product focus assertion failed; `focus-diagnostic.txt` records that the former virtualized row is disconnected and the stable active-document breadcrumb is available | actual product replacement checks in `authenticated-product-final.txt` |
| Real conflict merge persists exact selected content and rejected save retains/downloads latest bytes | The UI contracts above were RED before implementation. The API persistence runner is integration confirmation, not claimed as an additional independent RED axis. | `authenticated-product-final.txt` |

The focused review repair removed each behavior/style slice before its reached RED and then restored the minimum implementation. No test was weakened or hidden to reach GREEN.
