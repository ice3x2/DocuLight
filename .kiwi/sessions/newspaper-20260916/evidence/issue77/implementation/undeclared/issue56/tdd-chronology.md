# Issue #56 TDD chronology

Requirement coverage: IR-STORAGE-001, FR-EDITOR-008, FR-STORAGE-003, FR-STORAGE-004. The frozen decision also preserves IR-SHELL-009 AC-6, IR-EDITOR-002 AC-2/AC-4, and FR-EDITOR-009 behavior.

1. `red-version-history.txt` was recorded before the VersionHistory/MergeView implementation. It reaches three intended behavior assertions: list failure was still shown as perpetual loading; a failed restore still invoked the success callback and supplied no retry alert; version comparison left its right CodeMirror pane editable and lacked the named raw-source/footer contract.
2. `green-version-history-initial.txt` records the first implementation run. It was not claimed as GREEN: it exposed a compatibility assertion and test matcher problems plus the still-editable left pane. These were corrected without weakening the intended assertions.
3. `red-restore-duplicate.txt` reaches `calls === 1` with two restore events batched before React rerender and fails with two requests. `restorePending` was then added as the synchronous operation lock.
4. The stale-response guard was removed after adding the late A→B test. `red-stale-comparison.txt` reaches the final selected-version assertion and shows the late 2판 response replacing the already-loaded 1판. The request identity guard was restored.
5. The complete #56 CSS implementation block was removed after the checked-in Playwright fixture/checker existed. `red-playwright-layout.txt` reaches the computed list-overflow assertion (`visible !== auto`). The CSS block was restored, then its list sizing was corrected to border-box after the next measured failure.
6. `red-playwright-restore-focus.txt` reaches the actual DocumentArea restore-success focus assertion and shows focus falling to an element with no accessible label. VersionHistory now focuses the existing document-header menu target immediately before its successful completion callback closes the panel.
7. After adding the conflict raw-source regression, the CodeMirror update listener was removed. `red-playwright-conflict-raw-source.txt` reaches the post-edit raw alternative assertion and fails because the hidden source retains the initial draft. The listener was restored.
8. `green-version-history-final.txt` is the final focused RTL run (14/14). `green-playwright-layout-final.txt` is the final checked-in Playwright 12-environment run using persistent Chromium plus `chrome.tabs.setZoom(2)` for the genuine 200% cases; its state pass also edits the shared conflict pane and checks the live complete raw alternative.
9. `red-shared-buttons-status.txt` reaches the missing polite compare status before checking the required shared button variants. Version controls were replaced with existing `Button` variants and the loaded comparison gained a concise polite status.
10. The expanded AppShell checker then reached a focus assertion and recorded `red-playwright-compare-focus.txt`: using the shared button's native disabled loading state moved focus away from the initiating compare control. The compare action now uses `aria-busy`/`aria-disabled` without removing it from focus while its request is pending.
11. After a visibility wait made the restore-error focus assertion stable, the retry-focus implementation was removed and `red-playwright-restore-error-focus.txt` reached the intended assertion with focus outside history. The minimal ref/effect focus restoration was reapplied. `green-playwright-layout-final.txt` covers both focus contracts on the final source.
12. The same independent reviewer found pathological real-200% rows. `red-playwright-row-wrap.txt` reaches the computed requirement that actions follow metadata on a new line and fails. Narrow-shell CSS now wraps the row and gives metadata/actions a full flex line; the final measurements keep every row within the list.
13. `red-duplicate-compare.txt` batches two clicks while the same comparison request is pending and reaches `compareCalls === 1` with an actual value of 2. The comparison request path now holds an in-flight set per sequence while still allowing rapid A→B selection.
14. `red-playwright-keyboard-traversal.txt` traverses actual DOM focus from the selected row through all remaining rows, comparison viewport and footer, and reaches the missing left/right pane assertion. MergeView now names both pane scrollers and places them in the tab sequence. The final checker also reverses with Shift+Tab and uses ArrowRight to scroll the narrow comparison viewport.
15. The third independent review identified the A→B→A pending-selection gap. `red-compare-a-b-a.txt` reaches the final A raw-source assertion and fails because the original per-sequence set returns before recording the latest A intent; B remains selected. The in-flight store now reuses the single A promise while attaching a fresh latest-intent identity to every selection. `green-compare-a-b-a.txt` records 16/16 focused tests passing.
16. The checker was strengthened before the cue fix to inspect each `.dl-diff-cue` by merge side, exact text, accessible name, role, and count. `red-playwright-diff-cues.txt` reaches the pure-insertion assertion with an unexpected left-side `− 삭제` cue. Cue generation now requires a real clamped source span and runs after CodeMirror constructs both panes. `green-playwright-diff-cues.txt` records the full 12-environment checker passing, including zero cues for empty/equal, only `+ 추가` for insertion, and only `− 삭제` for deletion.

The initial checker was refined after its behavioral RED to move screenshots before successful restore closes the history panel and later to wait for the visible restore retry before inspecting focus. The stable focus checker was explicitly rerun RED with its affected implementation removed before restoration. The final checker was rerun after all refinements.
