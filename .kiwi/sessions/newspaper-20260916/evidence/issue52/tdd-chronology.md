# Issue 52 TDD chronology

All paths below are raw command stdout captured during this implementation. Product code was not hidden and tests were not weakened to obtain RED.

| Contract | RED before the corresponding implementation | GREEN |
| --- | --- | --- |
| Visible label/help, result hierarchy, idle/loading/empty/error, axes-empty guidance | `red-rtl-search-states-layout-ime.txt`: assertions reached missing `aria-describedby`, idle copy, and axes-empty copy | `green-rtl-search-states-layout-ime.txt` and web full 711/711 |
| 36px input/filter, 48px filter width, 8px gap, result-only scroll, 240px popover, IME boundary | `red-playwright-search-geometry.txt`: actual AppShell input measured 21px against 36px | `green-playwright-search-12env.txt`; `green-measurements.json`; 12 light/dark/viewport/zoom environments |
| App → AppShell → SearchPanel real query loading/error/refetch | `red-app-search-state-wiring.txt`: after removing only the new state prop delivery, the test reached and failed the missing “검색 중…” assertion | `green-app-search-state-wiring.txt`; final `search-wiring.test.tsx` also covers stale response isolation |
| Visible label is programmatically tied to the cmdk-generated input id | `red-visible-label-association.txt`: `for=search-query` differed from cmdk input `radix-…` | `green-visible-label-association.txt` |
| Actual service/HTTP AND, OR, minimum length, parentheses, cross-axis matching, result dedupe | `red-server-search-query-integration.txt`: four assertions failed on the previous literal-includes implementation | `green-server-search-query-integration.txt`: 30/30 |
| Complex PDF query extracts once; rejected query and axes-zero extract zero times | `red-server-search-cost.txt`: with the relevant one-pass loop removed, the existing test reached `calls=2` versus expected 1 | `green-server-search-query-integration.txt`: restored one extraction pass |
| Cmdk virtual keyboard focus has a non-color 2px focus indicator, remains unclipped, and pointer movement does not masquerade as keyboard focus | `review-red-keyboard-virtual-focus.txt`: the actual AppShell/SearchPanel kept DOM focus in the input while the selected result computed `outline-style: none`; `review-red-pointer-vs-keyboard-state.txt`: after temporarily removing only the pointer-mode reset, the already-present assertion reached the selected result and observed a solid outline after pointer movement | `review-green-playwright-12env.txt`: checked-in Playwright checker passed all 12 environments, including Arrow navigation, unclipped outline, pointer distinction, IME boundary, and Enter open exactly once |
| Overlapping OR terms share one excerpt only when they identify the same underlying body match | `review-red-overlap-identity.txt`: permanent service and Express HTTP tests reached `alpha | alph` and received 3 excerpts instead of 2 | `review-green-overlap-identity.txt`: service and HTTP suites passed with identity based on source offsets while retaining a second distinct `alpha` occurrence |
| Unchecked filter hover uses the control surface while checked state remains native and selected | `review-red-filter-hover-token.txt`: the actual Radix popover option computed the selected-surface color instead of the control-surface color | `review-green-playwright-12env.txt`: unchecked hover resolved to `--surface-control`; checked name remained checked/selected |
| Virtual result focus ends when DOM focus leaves the cmdk input | `rereview-red-stale-virtual-focus.txt`: after Tab focused the real filter `BUTTON`, the checker reached the lifecycle assertion and observed `data-keyboard-navigation="true"` | `rereview-green-playwright-12env.txt`: Tab and pointer transitions clear the result outline while Arrow/Home/End and Enter remain operational |
| The full 2px outline plus 2px offset fits inside the result scroll viewport | `rereview-red-outline-clipping.txt`: the first selected row reached the geometry assertion with its 4px left/top/right extent outside the scroll viewport; the following run also reached genuine 200% and exposed a tall fixture row that could not physically fit the reduced viewport | `rereview-green-playwright-12env.txt`: first, middle, and last representative rows pass left/right/top/bottom checks at all six 100% and six genuine 200% environments using 4px viewport padding and scroll margins |
| OR branch order does not change document or excerpt output | `rereview-red-or-order.txt`: permanent service and Express HTTP assertions reached `alpha | alph` versus `alph | alpha` and showed the first excerpt differed by one character | `rereview-green-or-order.txt`: 32/32 tests pass; representatives prefer the longer match at one source identity and results retain stable source order |
| #51 virtualized tree regression targets are attached and visible before interaction | `independent-rereview-2.md`: the independent reviewer preserved the fresh-process raw Playwright failure at `treeitem nth(2) Element is not visible` and reproduced FAIL/PASS/PASS before this checker edit | `rereview3-green-tree-five-runs.txt`: the same checked-in checker passed five consecutive fresh processes, retaining the genuine zoom and geometry assertions |

The earlier `http-search-query-contract-gap.txt` was a diagnostic RED before Astra expanded the issue scope. Its temporary test was replaced by the permanent service and Express HTTP tests represented in `red-server-search-query-integration.txt`.

The checked-in Playwright checker was authored before the CSS/product layout implementation. A later focus assertion initially raced Radix focus restoration; it was refined to wait for the meaningful active-element condition. This refinement did not introduce product behavior.

For the review additions, each raw RED above reached the specific behavior assertion before its corresponding product edit. `review-red-pointer-vs-keyboard-state.txt` was produced by removing and then minimally restoring only the new pointer reset; no test or fixture was hidden or weakened. The service/HTTP overlap assertions are permanent tests, and the Playwright assertions remain in the checked-in checker.

The rereview clipping checker originally used long, naturally growing content for every row. At genuine 200% one row was 475px tall inside a 165px result viewport, so its entire rectangle could not physically be visible at once. The fixture now keeps long Korean, unbroken strings, many rows, and multiple excerpts in the matrix while using concise first/middle/last rows for the explicit whole-outline geometry checks. This is a test-data isolation change; the four-edge assertions and natural row growth remain unchanged.

The third-review checker fix changes verification infrastructure only. After inline naming detaches, the checker moves focus to the tree so the accessible long-name tooltip no longer intercepts pointer input. `visibleTreeRow` waits for a connected, non-zero, visible virtual row, scrolls only when it is not already visible, and returns a newly resolved locator. It stores no `ElementHandle`, uses no fixed sleep or retry wrapper, and leaves every product and geometry assertion intact.
