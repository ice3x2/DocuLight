# Issue 53 TDD chronology

All times are local workspace times on 2026-09-17. Raw command output is preserved in this directory.

| Behavior contract | RED evidence before its product implementation | Minimal implementation | GREEN evidence |
| --- | --- | --- | --- |
| Link rows expose the newspaper structure, full wrapping names, workspace only when supplied, unresolved text with generic `미해결`, and named empty state. | `red-rtl-panels-navigation.txt` fails the new `link-panel.test.tsx` assertions because the structure and empty copy do not exist. | `LinkPanel.tsx` adds semantic row content and the empty state without adding actions or metadata to unresolved rows. | `green-rtl-panels-navigation.txt` |
| Tag panel keeps a fixed scope/current-scope/basis region, list association, count column, and honest empty state. | `red-rtl-panels-navigation.txt` fails the new `tag-panel.test.tsx` assertions because the fixed region and named rows do not exist. | `TagPanel.tsx` adds the fixed region and ordered server-provided tag rows. | `green-rtl-panels-navigation.txt` |
| Loading and error replace stale tag rows while retaining fixed scope controls; retry uses the supplied query refetch. | `red-rtl-tag-fixed-states.txt` reaches the state assertion and fails because the ready rows remain visible. | `TagPanel.tsx` receives a small query-state union and renders shared loading/error state outside the fixed controls. | `green-rtl-panels-navigation.txt` |
| Resolved backlinks/outgoing links activate the exact recursively nested node ID by pointer or keyboard; unresolved rows remain noninteractive. | `red-rtl-panels-navigation.txt` reaches the nested activation assertions and fails because `AppShell` scans only root nodes. | `AppShell.tsx` resolves IDs through the existing recursive `nodeById` helper and passes the exact node to the existing document-open path. | `green-rtl-panels-navigation.txt` |
| Link and tag query errors beat stale successful data; loading/empty/error/retry are distinct and no-document link state is neutral. | `red-app-query-wiring.txt` is the valid RED. The initially added App wiring was removed, then the actual App test was run; it reaches the safe error assertion while rendering stale `오래된 링크`. | `App.tsx` maps existing React Query `isError`, `isFetching`, and `refetch` values into the minimal shell state props, testing error first. `AppShell.tsx` renders shared states. | `green-app-query-wiring.txt` and `green-rtl-panels-navigation.txt` |
| The backlink list has computed vertical overflow; the tag header stays in place after assigning the tag list scroll offset; link targets are at least 36px, tag targets are at least 40px, and asserted focus/token/grid properties hold across required themes, viewport sizes, and zoom. | `red-playwright-right-panel.txt` reaches the computed backlink-list overflow assertion and reports `visible` where `auto` is required. | `shell.css` adds the right-panel flex shrink chain, list overflow, row geometry, wrapping styles, focus outline, fixed tag header, and two-column tag count styling. The checker does not assert wrapping, exact outline width/offset, count right alignment/non-overlap, or a nonzero tag-list scroll offset. | `green-playwright-right-panel.txt`, `green-measurements.json`, and the 12 `green-right-*.png` screenshots |
| Successful empty panels reuse the shared EmptyState, and a focused row/retry removed by state, scope, or document transition restores focus to the active right tab. | `fixer-red.txt` records 4 failures: two missing shared empty-state ancestors and two focus losses to body. | `LinkPanel.tsx` and `TagPanel.tsx` reuse `EmptyState`; `AppShell.tsx` adds a state-identity boundary that restores focus only when the removed subtree held it. | `fixer-green.txt` (targeted 36/36, full web 721/721, browser retry-success focus check) |

The final browser checker was refined after the first GREEN attempt only to make the tag-row locator unambiguous (`data-testid=tag-row` followed by its button). That edit did not add a behavior contract or change product code. No RED claim relies on that locator refinement.
